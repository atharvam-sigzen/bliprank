# MVP audit against PRODUCT_GOAL.md — 2026-09-09

Verified gap list. Every line was checked in code or by running it; nothing
here is taken from prior docs. Read `docs/PRODUCT_GOAL.md` first. Point
numbers below are its point numbers. A builder session works from this list
and updates `docs/MVP_PLAN.md`; it does not re-derive the list.

Ground truth at audit time: `pnpm test` 114 files / 1576 tests pass, offline.
`pnpm typecheck` passes. Real data on disk for three domains (pipedrive.com,
sigzen.com, thecosmicbyte.com): 360 answers, 399 calls, ~$2.14, last
collected 2026-09-01. No domain has a second cycle.

## Verdicts

| Point | Verdict | One-line reason |
|---|---|---|
| 1 Core product | PARTIAL | Measurement is real and correct; nothing sellable (no identity, billing, server state). |
| 2 Two personas | PARTIAL | Separate chrome; agency client list is localStorage; invented clients rendered. |
| 3 Any real domain | PARTIAL | Real content classifier and growing taxonomy; 14 curated banks carry hand-authored competitor sets; G3 labelled set absent. |
| 4 Dual-audience | PARTIAL | One depth toggle over the same data; raw evidence hidden at the default brand depth. |
| 5 Transparency | PARTIAL | Verbatim answers reachable; CI-band visuals; evidence route local-only; three insights still tables. |
| 6 Recurring operation | NOT SATISFIED | No scheduler, loop never run, no second cycle anywhere. |
| 7 Correctability | SATISFIED (local-demo boundary) | Versioned corrections, operator-applied; no identity. |
| 8 Bounded scope | SATISFIED | No generation or publishing path. |
| 9 Integrity | PARTIAL | Main path gated eightfold; two unbudgeted paid paths; no DEFF; RLS layer unused. |
| 10 Engineering | PARTIAL | Zero TODOs, correct SSRF pin, real tests; spoofable throttle, hook no-op, no CI/lint/e2e. |
| 11 Complete delivery | PARTIAL | Deep where built; most of the product surface unbuilt. |

## Evidence per point

### 1
- Wilson correct, property-tested: `packages/stats/src/wilson.ts:49-54`, `wilson.test.ts:18-117`. n=0 throws.
- Five real adapters, one provider: `services/collector/src/adapters/openwebninja.ts:68-97`.
- Deterministic scorer, no model call anywhere in `services/scorer`; `SCORING_ALGO_VERSION='det-3'` at `score.ts:24`, stamped at `:402`.
- R8 shape on every metric: `packages/stats/src/format.ts:23-46`; built at `services/grader/src/scan.ts:749`.
- No auth: zero hits for session/cookies/supabase in `apps/**`. No stripe/razorpay dependency. All `/api/*` routes marked LOCAL DEMO ONLY and absent on the static deploy.
- CI is nominal: `wilson(mentions, n)` with raw n; `wilson.ts:11-15` says callers must apply n/DEFF; no DEFF anywhere. G0 unrun.
- Golden set 7 of 300; G2 reports NOT_RUN (`services/scorer/golden`).

### 2
- Separate chrome: `apps/public/components/chrome.tsx:389-394`. Role = localStorage string, gates nothing (`chrome.tsx:211-216`).
- Agency clients = `localStorage['bliprank-agency-domains']` (`apps/public/lib/workspace.ts:222,299-334`).
- Six invented clients always rendered: `apps/public/app/agency/page.tsx:252-327`, `lib/agency-fixture.ts:396-406`.
- Per-client view is the brand `WorkspaceRecord` with copy changes (`workspace-record.tsx:296,476,486`). Lifecycle page: 4 of 5 stages `<Planned />`.
- `packages/db` not a dependency of `apps/public`.

### 3
- Ladder: `services/grader/src/resolve-category.ts:812-960`; homepage fetched via `fetch-site.ts:125-190`; content scoring `packages/taxonomy/src/classify-content.ts:271-314,366-397` with hard-coded thresholds calibrated from one fixture.
- Growth: bank author `bank-author.ts:480-501`; generated banks stored write-once; `leaders: []` forced at `resolve-category.ts:753`; stored banks with leaders dropped at `:447`.
- Promotion from answers only: `promote-competitors.ts`.
- GAP: 14 curated banks ship hand-authored `leaders` marked "never collected" (`packages/taxonomy/src/banks/*.ts`, e.g. `crm-software.ts:21-30`); used as the competitor set for position scoring.
- GAP: no labelled 100-domain file exists; harness at `label.ts`/`label-run.ts` never fed.
- GAP: no author key → fallback record written permanently (`resolve-category.ts:319-331,898-900`).

### 4
- Depth mechanism: `apps/public/app/globals.css:3283`, `components/theme.tsx:45,247-249,285-290`; tested by `lib/depth.test.ts`.
- GAP: `<Evidence>` at `components/prompt-breakdown.tsx:289` sits inside the `.detail` div opened at `:161`; simple is default off `/agency`.

### 5
- Raw path: `prompt-breakdown.tsx:337` → `lib/answers.ts:224-228` → `/api/answers` → `<pre>` at `prompt-breakdown.tsx:392-431`; text never excerpted (`services/grader/src/answers.ts:83,253`).
- Visuals (hand-rolled SVG/CSS, no Visx): `range-rail.tsx`, `ci-trend-chart.tsx:101-125`, `head-to-head-chart.tsx:41-90`, `prompt-breakdown.tsx:555-600,640-700`. R8 verdict via `packages/stats/src/format.ts:165-211`, consumed at `lib/cycles.ts:86-95`.
- GAP: `/api/answers` local-only (`route.ts:30-33`). Per-prompt grid, cited sources, gap coverage are tables. `range-rail.tsx:101-109` formats bounds inline. CLAUDE.md §6 claims Visx.

### 6
- No `.github/`, `vercel.json`, cron, wrangler, QStash registration. `daily-loop.ts:47` "does not register itself"; refuses unless `GRADER_DAILY_LOOP=armed` (`:222`).
- No `tracked.json`, no `daily-spend.json`, no `results/cycles/` on disk.
- Pricing discloses it: `apps/public/app/pricing/page.tsx:58-59,198-199`.

### 7
- Category: `resolve-category.ts:361-404` new version + history, never re-derives. Competitors: `competitor-overrides.ts:126-147`, basis `set=N`. Prompts: `custom-prompts.ts:120-184`, basis `custom=K@V`.
- Routes only file requests (`api/category/route.ts:22-24` etc.); apply is CLI. No identity.

### 8
- Repo grep for generate/publish/cms/wordpress: only queue `publish()` in `qstash.ts:106`, internal tool name `publish_category`, localStorage "draft". `aeo-audit.test.ts:157-160` pins no model transport.

### 9
- Scan gates in order: `apps/public/app/api/scan/route.ts:98-102,303,313,330,340-344,355,365`; `collect-cell.ts:144,207,219-223`; `budget.ts` charges before every attempt.
- GAP: `services/grader/src/diagnose.ts:30-75` fetches all five paid endpoints with the key; no Budget/ledger; gate is only `COLLECTION_ENABLED`, which agent sessions inject.
- GAP: `bank-author.ts:361,419` model calls have request caps, no dollar ledger; reachable from public `/api/preview` (`route.ts:184-190`).
- GAP: `rescore.ts:148-235` has no same-version guard (sigzen.com.json was rewritten det-3→det-3, additive only, audit copy kept).
- GAP: DEFF unapplied (see 1). `packages/db` RLS: 88 tests pass, zero consumers.

### 10
- Clean: 0 TODO/FIXME/ts-ignore in source; 7 `ponytail:` ceilings, each with upgrade path.
- SSRF: `fetch-site.ts:191-194` refuses if any resolved address internal; `:412-433` pins connect to validated IP; manual redirects re-resolved; 512 KB cap; 6 s timer.
- GAP HIGH: `services/grader/src/visitor-throttle.ts:67-83` trusts `cf-connecting-ip`, then `x-vercel-forwarded-for`, `x-real-ip`, `x-forwarded-for`, unconditionally. Spoofable off Cloudflare; `visitor-throttle.test.ts:35` asserts the precedence.
- GAP HIGH: `.claude/hooks/pre-spend.sh` blocks only when `COLLECTION_ENABLED!=true` (injected true in `.claude/settings.json:4`); its patterns never match `pnpm grader:scan` or `grader:tick`.
- GAP MED: no CI, no linter (three `eslint-disable` comments for an uninstalled linter), zero e2e tests (playwright only in `scripts/*.mjs`).
- GAP MED: DNS pin never tested; every test injects `fetchImpl` (`fetch-site.ts:311,402-405`).
- GAP MED: `blockedReason` misses non-canonical IPv6 (`0:0:0:0:0:0:0:1`, `::ffff:7f00:1`, `2002:7f00:1::`).
- GAP LOW: undici Agent per redirect hop, only last closed (`fetch-site.ts:311,389`). `/api/scan` and `/api/preview` skip `isHost`. `preview/route.ts:242`, `scan/route.ts:432` return raw error messages.
- GAP: untested production files: `apps/public/lib/live-scan.ts`, `lib/preview.ts`, `components/new-cycle.tsx`, `scan-progress.tsx`, `prompt-preview.tsx`, `cap-split.tsx`, `range-rail.tsx`, `services/grader/src/tick.ts`, `diagnose.ts`, `aeo.ts`, `label-run.ts`.
- Map vs disk: `services/reconcile` does not exist; `services/grader` and `packages/taxonomy` not in the map; `apps/web` is untracked build residue; scorer's sampled sentiment path does not exist.

### 11 — half-states the code names itself
Daily loop unarmed and unregistered · no identity · no billing · all `/api/*` local-demo · abuse controls "P3.6" absent · publisher registry built not wired · sentiment unbuilt · reconciliation engine absent · stats thresholds PROVISIONAL (`format.ts:53-61,308-314`) · curated leaders "placeholder" · no gate from G0 onward ever passed.

## Ranked defects (code-only, no decision needed)
1. Spoofable client-IP header (10).
2. Unbudgeted paid calls in `diagnose.ts`; unmetered bank-author path behind public preview (9).
3. Pre-spend hook does not cover grader commands and is disabled by the injected flag (9, 10).
4. Evidence hidden at default brand depth (4, 5).
5. Same-version rescore guard missing (9).
6. Redirect agent leak; raw error leakage; `isHost` skipped on the two spending routes; IPv6 forms (10).
7. `range-rail` inline formatting; CLAUDE.md map and Visx claim wrong (5, 10).

## Gaps needing an owner decision or human-owned work
- Server-side state and identity (1, 2, 7): all state is JSON under `services/grader/data-live` on one machine.
- Where dynamic routes run (1, 5, 6): the static Cloudflare Pages deploy has no `/api/*`.
- Scheduler host and arming (6).
- Billing provider and entitlement (1).
- Curated-bank competitor sets: detection dictionary vs. ranked set (3; scoring rule set is human-owned).
- G3 labelled 100 domains (3): labels are a human's.
- G0 pilot and DEFF (1, 9): `packages/stats` is human-owned; the pilot spends.
