# MVP plan — BlipRank, from the 2026-09-09 audit

> Goal points are `docs/PRODUCT_GOAL.md` numbers; verdicts and gap lines are `docs/MVP_AUDIT_2026-09-09.md`, not re-derived. Update the Status column at the end of every session.

## Context

The audit leaves 9 of 11 goal points PARTIAL or NOT SATISFIED. The owner fixed the stage order and decisions D1–D7. This plan has one row per work item; **Stage A was executed on 2026-09-09** (code-only defects, no decisions; branch `mvp/stage-a`). Stages B–E are planned to the file level so the next session takes the first incomplete stage without re-auditing.

Branch: `mvp/stage-a` off `fix/preview-every-domain` HEAD (`76ffa65`). First commit: the owner's uncommitted `docs/PRODUCT_GOAL.md`, `docs/MVP_AUDIT_2026-09-09.md`, the CLAUDE.md pointer line, and this plan as `docs/MVP_PLAN.md`. One conventional commit per row after that, `[P3]` tag, never on main.

Status values: `TODO` · `DONE` · `DONE ⚠️ HUMAN REVIEW` · `BLOCKED (owner)`.

## Goal scorecard — the MVP is done when every row reads CONSISTENT

Verdicts are the oversight session's, verified against code, not a builder's self-report. **Every session updates this table at its end**, alongside the Status column: change a verdict only with file:line evidence, and name the rows that remain. The 2026-09-09 column is the audit baseline.

| Point | 2026-09-09 | Now (2026-09-15) | What moved it | What remains (rows) |
|---|---|---|---|---|
| 1 Core product | PARTIAL | PARTIAL | Identity, server state and a per-workspace store exist (B2, B3a, B3b) | Billing (D2); DEFF and the G0 pilot (E3); B3c |
| 2 Two personas | PARTIAL | PARTIAL | Account kinds; a brand owns one workspace by trigger (B2); the shared ledgers no longer name or cap across workspaces (B3c items 1, 2: `live-gate.ts:263`, `gaps/route.ts:101`, proved at `session-store.test.ts:329,352`) | Agency portfolio from real workspaces, fixture deleted, agencies no longer refused (D1). The cycle ceiling now admits per workspace (B3d item 1: `domain-ceiling.ts:111`, proved through `/api/scan` at `session-store.test.ts:416`); the burst cap's repeat exemption stays keyed by bare host, recorded as accepted in ADR-0017 Amendment 2 because it only admits a cached re-show |
| 3 Any real domain | PARTIAL | PARTIAL | nothing yet | Curated leaders become a detection dictionary (E1); labelled 100 (E2, owner) |
| 4 Dual-audience | PARTIAL | CONSISTENT | Evidence reachable at simple depth (A4) | D4 improves it; nothing blocks |
| 5 Transparency | PARTIAL | PARTIAL | Evidence served per workspace on the deployment (B3b) | Three table-only insights (D4) |
| 6 Recurring operation | NOT SATISFIED | NOT SATISFIED | Ledgers and cycles can live off one machine (B3b); the tick lock is a lease in the store and the daily ledger folds, so two instances cannot double-spend a day (B3c item 3: `daily-loop.ts:164,271`, proved over the KV double at `daily-loop.test.ts`). **The transport exists (C1, C2):** ADR-0018; the fan-out and the domain job run the loop's one per-domain path (`daily-loop.ts:541,738,819`), the route verifies before it reads (`lib/tick.ts:127`), the whole path is proven offline through the real route and runner and over PGlite (`api/tick/route.test.ts:196,321`); `pnpm grader:schedule` registers the one schedule (`schedule.ts:122`). Nothing is registered or armed, and nothing has run daily | Human review of C2 (⚠️); C3 (the deployment's tracked list has no write path, so a fan-out there publishes nothing); then the owner registers the schedule and arms the deployment; `SCHEDULE_FACT` changes only after a live tick has filed a cycle |
| 7 Correctability | SATISFIED (local) | CONSISTENT | Operator apply inside the workspace, version N+1 checked in the database, 409 on a lost race (B4, tested at `session-store.test.ts:311`); the role rides in the token, is verified against membership and the two applying writers refuse a member at the database (B3c item 8: `0005_workspace_role_claim.sql:181-182,227,256`, `token.ts:32`) | Human review of migrations 0005 to 0007 (⚠️). B3d decided and built: a first category record is any member's write and corrections stay owner/admin (`0006_member_first_record.sql:62`, proved at `session-store.test.ts:316`); a filing is the filer's, a member cannot displace another's (`0007_filing_ownership.sql:53`, `session-store.test.ts:344`) |
| 8 Bounded scope | SATISFIED | CONSISTENT | unchanged | nothing |
| 9 Integrity | PARTIAL | PARTIAL | Every paid path charges first on both backends; same-version guard; RLS layer has real consumers (A2, A5, B3b); admission and booking are one exclusive write, the file ledgers open only for a declared single process, a stray cap file cannot set a deployment's cap (B3c items 3, 4, 5: `daily-loop.ts:164`, `ledger-stores.ts:69`, `live-gate.ts:65`) | DEFF and G0 (E3); human review of the spend-control changes (⚠️), now including the per-workspace ceiling (B3d item 1) and C2's job-shaped reservation, the day opened before any job is published, and the shared live gates (`daily-loop.ts:276,332,485`; every non-2xx of the route provably before a spend, `lib/tick.ts`) and the deploy check's derivation over every definer writer of workspace state, dynamic SQL refused (B3d item 3: `check-deploy.sql:164`, six failing databases at `check-deploy.test.ts:300`) |
| 10 Engineering | PARTIAL | PARTIAL | CI; SSRF and throttle fixes; deploy gate derives over everything a tenant can read (A1, A6, B0, B3r); a provider body never reaches a visitor and the lost-race path is tested (B3c items 6, 7: `live-gate.ts:280`, `scan/route.ts:348`, `session-store.test.ts:66`) | No browser or e2e test; deploy runbook for the CREATEROLE walk (owner). The full suite's exit code no longer depends on load: the worker RPC timeout was measured to the hook test's 34 synchronous bash spawns and removed (B3d item 6: `pre-spend-hook.test.ts:102,131`, three clean full runs in an isolated worktree). **B5 closed it (2026-09-15):** the route tests run over scratch directories and no test names the live directory (`build-env.test.ts:100`); a clean worktree passes 131 files and CI run 34963093487 is the branch's first green run, so the gate B0 built has now actually passed remotely |
| 11 Complete delivery | PARTIAL | PARTIAL | Stage B built; Stage C's ADR and transport built (C1, C2) | C2's review, C3; Stages D, E |

**Owner-only work the MVP cannot finish without:** the human reviews on every ⚠️ row; provisioning Supabase, Vercel, R2 and Upstash and setting the deploy variables listed in CLAUDE.md §7; the 100 labels (E2); the G0 pilot go-ahead and spend (E3); arming the daily loop after C2.

---

## Stage A — confirmed defects (audit "Ranked defects" 1–7)

| # | Goal | Item | Files | Test that proves it | Status | Human |
|---|---|---|---|---|---|---|
| A1 | 10 | Trusted-proxy allowlist for the client IP | `services/grader/src/visitor-throttle.ts`; the six callers of `extractClientIp` (`api/{category,competitors,custom-prompts,gaps,preview,scan}/route.ts`) pass `env` | `visitor-throttle.test.ts`: under `TRUSTED_PROXY` unset every header is ignored; `cloudflare` reads only `cf-connecting-ip`; `vercel` reads only `x-vercel-forwarded-for`; a non-IP value falls back | DONE | no |
| A2 | 9 | `diagnose.ts` behind `Budget` + the shared `ledger.json`; every model call in `bank-author.ts` behind a `Budget` on `bank-author-ledger.json` | `services/grader/src/diagnose.ts`, `bank-author.ts`, `bank-author.test.ts`, callers of `bankAuthorConfig` (`aeo.ts`, `/api/preview`, `/api/scan`), `CLAUDE.md §7` | new `diagnose.test.ts`: charge precedes fetch, exhausted ledger ⇒ zero fetches; `bank-author.test.ts`: ledger file written per attempt, cap reached ⇒ `null` and no fetch | DONE ⚠️ HUMAN REVIEW | ⚠️ spend control |
| A3 | 9, 10 | `pre-spend.sh` covers grader spend commands and no longer keys on `COLLECTION_ENABLED` | `.claude/hooks/pre-spend.sh`, new `services/grader/src/pre-spend-hook.test.ts` | hook test spawns bash+jq: `pnpm grader:scan` blocked, `-- --fixture` allowed, `grader:tick -- --apply --live` blocked, dry `grader:tick` allowed, `grader:diagnose` and `grader:aeo` blocked, all with `COLLECTION_ENABLED=true` in env | DONE ⚠️ HUMAN REVIEW | ⚠️ spend control |
| A4 | 4, 5 | Raw evidence reachable at simple depth | `apps/public/components/prompt-breakdown.tsx` | `prompt-breakdown.evidence.test.tsx`: the evidence button survives `stripToSimple` (lib/simple-view) | DONE | no |
| A5 | 9 | Same-version rescore guard | `services/grader/src/rescore.ts`, `rescore.test.ts` | a stored result already at `SCORING_ALGO_VERSION` is SKIPPED unless `--same-version` (ADR-0012's additive case) is passed; arg parser test | DONE | no |
| A6 | 10 | Redirect agent leak · raw error leakage · `isHost` on both spending routes · IPv6 forms | `services/grader/src/fetch-site.ts`, `fetch-site.test.ts`, `apps/public/lib/host.ts` (new: `isHost` + the route `normalise`, replacing the copies in six `/api/*/route.ts`; `lib/workspace.ts` already has a `normaliseHost` — reuse it if it is the same function) | `fetch-site.test.ts`: `0:0:0:0:0:0:0:1`, `::ffff:7f00:1`, `2002:7f00:1::`, `64:ff9b::7f00:1`, uncompressed mapped form all blocked; pin closed per hop (spy); `scan/route.test.ts` + `preview/route.test.ts`: a thrown internal error yields a fixed message, and `domain: "not a host"` is a 400/`input` error | DONE | no |
| A7 | 5, 10 | `range-rail` bounds through `packages/stats/format`; CLAUDE.md map and Visx claim corrected | `packages/stats/src/format.ts` (+`formatBounds`), `format.test.ts`, `apps/public/components/range-rail.tsx`, `CLAUDE.md` §2 map, §6 stack | `format.test.ts`: `formatBounds` agrees with `formatInterval`; grep test: no `toFixed(` in `range-rail.tsx` | DONE ⚠️ HUMAN REVIEW | ⚠️ packages/stats (one formatter) |

### Stage A design notes (decisions made, so implementation is mechanical)

**A1.** `extractClientIp(req, env = process.env)`. `TRUSTED_PROXY` ∈ `cloudflare | vercel | unset`. Cloudflare: `cf-connecting-ip` only — its doc: "provides the client IP address connecting to Cloudflare", and recommends it over `X-Forwarded-For`, which Cloudflare *appends* to (developers.cloudflare.com/fundamentals/reference/http-headers, fetched 2026-09-09). Vercel: `x-vercel-forwarded-for` only — its doc: Vercel overwrites `x-forwarded-for` and "do[es] not forward external IPs"; `x-vercel-forwarded-for` "is identical … however `x-forwarded-for` could be overwritten if you're using a proxy on top of Vercel" (vercel.com/docs/headers/request-headers, last updated 2025-12-13). Unset: no header is read; the key is `direct`, so every caller shares one bucket — fail-closed, and the preview route's global cap already carries the real bound. A trusted header whose value is not an IP (reuse `parseIpv4` from fetch-site, or contains `:`) falls back to `direct`. Existing precedence test is replaced, not kept.

**A2.** Diagnose: `new Budget(join(dataDir,'ledger.json'), ledgerCapUsd(dataDir, env), e => PRICE_USD_PER_CALL[plan][e], () => new Date(), engines.length)` and `budget.charge(engine)` before each fetch; also requires `GRADER_LIVE_SCAN=true` like every other live path. Extract `runDiagnose({engines, key, budget, fetchImpl, log})` so the test injects fetch. Bank author: `BankAuthorConfig` gains `ledger: { file, capUsd, usdPerCall }`; `bankAuthorConfig(env, readKey, dataDir)` (third param required; ten test call sites updated). `askText` and `askAnthropic` both open the Budget and `charge(model)` before the network call — one guard where both providers and the AEO drafter route through. `BudgetExceeded` is caught in `authorBank` like any failure ⇒ `null` ⇒ general bank. Env: `BANK_AUTHOR_USD_PER_CALL` (default `0`: the default models are free tiers, the ledger still counts calls), `BANK_AUTHOR_CAP_USD` (default `5`; an existing file's cap wins, same rule as `ledgerCapUsd`).

**A3.** Remove the `COLLECTION_ENABLED` condition entirely: a matching command is blocked in an agent session, full stop; a person runs it from a plain shell. Patterns added: `grader:(diagnose|aeo)`, `grader:scan` unless the same command carries `--fixture` or `--stub`, `grader:tick` only with `--live`, and the `tsx services/grader/src/(run|diagnose|aeo|tick).ts` forms with the same exceptions.

**A4.** Move `<Evidence …/>` from inside the `.detail` wrapper to directly after it. The sentiment caveat stays inside. `depth.test.ts` rules 1–4 are unaffected (no new `.detail`, no prose added to simple).

**A5.** In `main()` after `planRescore`, `if (plan.algoVersion === SCORING_ALGO_VERSION && !o.sameVersion)` ⇒ SKIP line naming R5 and ADR-0012. `parseRescoreArgs` learns `--same-version`.

**A6.** fetch-site: `await pin?.close()` before `pin = pinnedAgent(...)` on each hop. `blockedReason`: add `expandIpv6(addr) → 8 hextets | null`; check unspecified/loopback on the expanded form; `::ffff:a:b` and `::a:b` (hextets 0–5 zero/ffff) ⇒ recheck embedded v4; `2002:` ⇒ v4 from hextets 1–2; `64:ff9b::/96` ⇒ v4 from hextets 6–7; fc00/fe80/ff00 on hextet 0. Routes: `catch (e)` logs `console.error` server-side and returns a fixed message. `isHost` after `normalise` in scan and preview via the new shared `apps/public/lib/host.ts`.

**A7.** `formatBounds(m, dp): { low: string; high: string }` (unit on `high` only, matching `formatInterval`). `BoundsRow` takes the `Metric` and prints `formatInterval` when narrow, `formatBounds` otherwise. CLAUDE.md: repo map adds `services/grader`, `packages/taxonomy`; marks `services/reconcile` "(not created yet; P4)"; scorer line says the sampled sentiment pass is not built; §6 "Visx (CI-aware charts)" → "hand-rolled SVG/CSS charts in `apps/public/components`".

---

## Stage B — foundation (points 1, 2, 7) — next session

| # | Goal | Item | Files | Test | Status | Human |
|---|---|---|---|---|---|---|
| B0 | 10 | CI: a GitHub Actions workflow runs `pnpm typecheck` and `pnpm test` on every push and pull request, offline (no provider key, no `COLLECTION_ENABLED`); the three `eslint-disable` comments that name an uninstalled linter are removed (no linter is added in this stage; strict `tsc` is the gate) | `.github/workflows/ci.yml` (new), `services/collector/src/blob-store.ts:74`, `rate-budget.ts:193`, `spend-ledger.ts:81` | the workflow file is valid and runs the two commands; grep test: no `eslint-disable` in source | DONE | no |
| B1 | 1, 5, 6 | D1: `apps/public` deploys to Vercel for the MVP; amend ADR-0002; `vercel.json` with function `maxDuration` per current Vercel docs; remove every "LOCAL DEMO ONLY / static deploy" comment that is now false | `docs/adr/0002-hosting-topology.md` (amendment), `apps/public/vercel.json`, six `api/*/route.ts` headers, `lib/answers.ts` 404 handling | `build-env.test.ts` extended: route files no longer claim absence on deploy | DONE | no |
| B2 | 1, 2 | D2: Supabase Auth magic link; `accounts` row per auth user; account owns workspaces; brand = one workspace, agency = many | `apps/public/lib/auth/*` (new), `middleware.ts`, `packages/db/migrations/0003_accounts_identity.sql` (account↔auth uid, account kind, one-workspace trigger, three onboarding definer functions), `packages/db/src/{schema,client,token,testing}.ts`, `app/{sign-in,account}`, `app/auth/confirm`, `app/api/{me,workspaces,auth/sign-in}` | `accounts-identity.test.ts` (15): a brand account is refused a second workspace at the trigger, the listing names only its own, a workspace is still read only through a verified token; `token.test.ts` round-trips the minter through `set_workspace_jwt`; `lib/auth/handlers.test.ts` runs the flow against the real migrations | DONE ⚠️ HUMAN REVIEW | ⚠️ RLS / tenancy; numbering clash with `fix/tenancy-deploy-gate`'s 0003 |
| B3 | 1, 7, 9 | D3: records, cycles, competitor overrides, custom prompt sets and requests move to Postgres tables scoped by workspace; raw answers to R2 (R4); `local-store.ts` replaced by a `Store` interface with the file store kept for tests and the CLI | **B3a (done 2026-09-10):** `packages/db/migrations/0004_workspace_state.sql` (three tables, SELECT policies on the verified context, four definer writers that take the workspace from `current_workspace_id()` and refuse without one), `schema.ts`, `services/grader/src/store/pg-store.ts` (the `WorkspaceStore` interface and its Postgres implementation over `@bliprank/db/client`), `services/grader/src/answer-stores.ts` (R2 + Upstash when all six variables are set, the file store when none, refused when partial; wired into `answers.ts`, `run.ts`, `rescore.ts`). **B3b (next):** the file modules (`cycles.ts`, `resolve-category.ts`, `category-requests.ts`, `override-store.ts`/`competitor-overrides.ts`, `custom-prompts.ts`) split into pure decisions plus a `WorkspaceStore` call, a `FileWorkspaceStore` over today's layout for the CLIs and tests, and the eight routes reading through a workspace token; **one configured store per deployment, refused when partial, never the file and Postgres stores authoritative at once** (their version sequences are independent, so a correction applied through one is invisible to the other's write-once check: the 0004 audit, Q6); the spend ledgers (`live-gate`, `daily-loop`, `domain-ceiling`, `visitor-throttle`, `bank-author`) still write files and keep `/api/scan` off on the deployment until they move to KV (⚠️ spend control) | `workspace-state.test.ts` (12: no context ⇒ no write, no direct write, same-version re-write vs new-version row, version N+1 per workspace, one pending per host, optimistic resolve, cross-workspace blindness); `tenant-isolation.test.ts` cases for the three tables; `pg-store.test.ts` (7, two real workspace contexts through `withWorkspace`); `answer-stores.test.ts`; grader suite unchanged on the file store | B3a DONE ⚠️ HUMAN REVIEW · B3b DONE ⚠️ HUMAN REVIEW (own row below) | ⚠️ RLS; R4; spend control (answer store wiring in the runner) |
| B3r | 1, 9, 10 | Oversight tenancy review of B2/B3a (2026-09-10, independent of the builder's audit; no leak found, three mechanism gaps): (1) wrap `0003_accounts_identity.sql` in a file-level `BEGIN`/`COMMIT` — a mid-file failure today leaves the migration owner a standing member of `svc_onboard`; (2) drop `app_rw` from the `ws_required()` grant (`0004:250`) so the wrapper can never appear in a tenant-facing policy the isolation test cannot see; (3) the two standing gates must assert the inverse: every relation `app_rw` can SELECT is either on an explicit shared-corpus allowlist (today `prompt_banks`) or carries a policy exercised by a disjointness case, and the derivation reads `with_check` as well as `qual` — a `USING (true)` table passes both gates today; (4) resolve the `0003` numbering clash with `fix/tenancy-deploy-gate` before either merges, and give `packages/db` a `schema_migrations` record or an ordered runner so two `0003`s cannot coexist silently | **Done 2026-09-10:** `0003` wrapped in `BEGIN`/`COMMIT` and opens with `schema_migrations` (name PK, unique index on the four-digit number, backfills 0000–0002, records itself; owner-only under FORCE RLS); `0004` records itself and grants `ws_required()` to `svc_onboard` only; `src/testing.ts` derives `MIGRATIONS` from the directory and every db test loads through it; `scripts/check-deploy.sql` asserts the record is gapless and indexed, and the inverse over every relation `app_rw`/PUBLIC can SELECT (any schema, any relkind, `with_check` read for INSERT/UPDATE/ALL, `prompt_banks` the one allowlisted name); `src/tenant-isolation.test.ts` derives its subject the same way (`uncovered()`); `src/rls.test.ts` sweep reads `with_check` and its adversarial subject is the inverse; new `src/migrations.test.ts` (numbering, record = files, a psql-faithful statement splitter, 0003 and 0004 cut after their GRANT). Observation, not changed: 0001 grants the owner `auth_verifier` and 0002 revokes it, a two-file arrangement by design; a database left between them fails the deploy check's owner-membership assertion. | `check-deploy.test.ts`: missing record / lost index / second file under 0003 / gap; open policy, wrapper policy, open `WITH CHECK`, a view with no policy. `tenant-isolation.test.ts`: `USING (true)` uncovered and both tenants see both rows; wrapper uncovered; a policy through `ws_required()` is permission denied for the tenant. `migrations.test.ts`: cut after the GRANT leaves no membership, and without the BEGIN it stands. | DONE ⚠️ HUMAN REVIEW | ⚠️ RLS / tenancy: migrations 0003 (section 0, transaction), 0004 (`ws_required` grant), `check-deploy.sql` (two assertions) |
| B3b | 1, 7, 9 | The file modules move onto the `WorkspaceStore` interface with a file implementation kept for the CLIs; the routes read and write through a workspace token from the verified session (`withWorkspace`, `mintWorkspaceToken`, `pgWorkspaceStore` gain their first non-test callers); one configured store per deployment; the spend ledgers (`ledger.json`, `live-cap.json`, `domain-ceiling.json`, `daily-spend.json`, `bank-author-ledger.json`) move off disk to a store the deployment can reach, with the same charge-before-attempt semantics and tests | **Done 2026-09-15** (ADR-0002 Amendment 2). Store: `store/file-store.ts` (`fileWorkspaceStore`, the interface over today's layout; reads through the modules' validated readers), `store/documents.ts` (record/override/prompt-set assembly, `recordsIn` for the resolver, `cycleInputOf`), `pg-store.ts` gains `sessionWorkspaceStore` (one `withWorkspace` per call) and `put(…, expectVersion)`; migration 0004's `ws_put_document` takes `p_expect_version` (write-once under the workspace lock). Modules: `checkCategoryRequest`, `checkOverrideWith`, `checkCustomPromptsWith` are the pure decisions; `fileCategoryRequestIn`, `fileCompetitorRequestIn`, `filePromptRequestIn`, `competitorsIn` are the store-based twins; `resolveCategory` takes `records` (`CategoryRecordStore`); `answers.ts`, `gaps.ts`, `run.ts`, `daily-loop.ts` take a store (file by default). Routes: `apps/public/lib/workspace-access.ts` is the one way in (identity on ⇒ the session's one workspace through a minted token; off ⇒ file store on a machine; a fleet runtime with identity off ⇒ 503; an agency with several workspaces ⇒ 409 until D1); all eight routes go through it, `lib/data-dir.ts` replaces eight copies of `resolveRoot`. Ledgers: `ledger-doc.ts` (`LedgerDoc`: file, or KV under a `setnx` lock; `CORRUPT` for the writer; `withRunAllowance`), `ledger-stores.ts` (`ledgerStores`: Upstash iff the six answer-store variables, files on a machine, refused on a fleet with neither; `spend()` = `Budget`/`LocalSpendLedger` on files, `KvSpendLedger` window `total` on KV); `live-gate`, `domain-ceiling`, `visitor-throttle`, `daily-loop`, `bank-author`, `run.ts` charge and count through them, same charge-before-attempt semantics, all four count ledgers now async. Also in this commit: the B3r tenancy audit's MAJOR (the tenant's reach is every role a tenant session can act as, walked with `pg_has_role(…, 'MEMBER')` excluding superusers, in `check-deploy.sql`, `tenant-isolation.test.ts`, `rls.test.ts`) and two MINORs (`relispartition`; the migration record written first). Not moved: generated banks (still files under `GRADER_DATA_DIR`, per instance on a deployment); the CLIs keep the file functions. | `store/file-store.test.ts` (7, incl. module readers ↔ store agreement), `pg-store.test.ts`, `workspace-state.test.ts` (+ expect-version), `ledger-stores.test.ts` (backend choice, fleet refusal, lock, CORRUPT, throttle on both backends, Budget and KV+allowance), `apps/public/lib/workspace-access.test.ts` (7), `apps/public/app/api/session-store.test.ts` (6: every route 401 with no session, the scan stream too; a body/query naming another workspace or account lands in the session's; the other account sees nothing; no account ⇒ 404, no workspace ⇒ 409); `check-deploy.test.ts` + `tenant-isolation.test.ts` gain the login-role and inherited-group cases; the existing route and ledger suites green on the file store | DONE ⚠️ HUMAN REVIEW | ⚠️ RLS / tenancy: 0004 `ws_put_document`, `workspace-access.ts`, the three derivations; ⚠️ spend control: `ledger-doc.ts`, `ledger-stores.ts`, `run.ts` (spend ledger), `bank-author.ts` (charge), the four ledger modules |
| B3c | 2, 9, 10 | Oversight reviews of B3b/B4 (2026-09-15, independent; database boundary holds, leaks are in the shared ledgers and in error text). Code-only, in this order: (1) `live-gate.ts:253` — the burst-cap refusal lists every domain scanned today on the deployment-wide ledger, i.e. other tenants' clients, verbatim to the caller; drop the list from the sentence (keep it on `used` for the server log); a test with two workspaces asserts no foreign host in any `/api/scan` error event, with both collection flags on and fetch stubbed. (2) `gaps/route.ts:96,108` — the per-domain cap is keyed by bare domain; key it `${workspaceId}:${domain}` like the three correction routes, with a two-workspace test. (3) `daily-loop.ts:156-179` — `takeTickLock` is a local-disk file, so on the KV backend two ticks can run at once; and `writeDailyLedger` (`:109`) ignores the value `update()` just read and overwrites with a stale snapshot, so two ticks can each authorise up to the full daily cap; move the tick lock onto `ledgers.doc()` (setnx + TTL, as `kvLedgerDoc` already does) and make every daily-ledger write fold into `current`; a KV-backend test with two interleaved ticks proves one refuses and the booked total never exceeds the cap. (4) `ledger-stores.ts:52-63` — fleet detection is marker-only and `spend()` overwrites `COLLECTOR_TOPOLOGY` with `single-process`; use `resolveTopology(env)` and require an explicit `COLLECTOR_TOPOLOGY=single-process` for the file backend; test: `COLLECTOR_TOPOLOGY=fleet` with no Upstash is refused. (5) `live-gate.ts:66` — read the local cap file only when `backend === 'file'`, mirroring `bank-author.ts:155`. (6) `live-gate.ts:264` — a provider error body is echoed to the visitor; fixed text. (7) a test for the 409 `read-again` path. **Human-owned, recorded here, not for a session to decide:** the token carries no role claim and no definer writer checks role (`token.ts:35-41`, `0004:183`), so role lives in one TypeScript boolean, `applies()`; the ceiling refusal is an existence oracle for other tenants' tracked hosts; the deploy check's role walk trips on a CREATEROLE deployer on PG16+ (fails closed; remedy is procedure, not an allowlist); the three 0003 definer functions take their subject as an argument. **Done 2026-09-15, items 1 to 8, four commits (`7f4cb4b`, `3186da6`, `9050d2d`, `b34dc9f`).** (1) the burst-cap sentence names no host; the list stays on `used` for the log (`live-gate.ts:263`). (2) the gap-report per-domain cap is keyed `${workspaceId}:${domain}` (`gaps/route.ts:101`). (3) the tick lock is a lease in the store's ledger document `tick-lock.json`, taken under the document's lock, reclaimed after six hours, refused when it does not parse (`daily-loop.ts:271`); every daily-ledger write folds into the stored value: `reserve` admits a domain and books its expected cost in one exclusive update, `settle` replaces the reservation with the realised figure, `unreserve` gives it back on a gate refusal, a host another tick has in flight is refused (`daily-loop.ts:142-200`). (4) `ledgerStores` chooses by `resolveTopology`: fleet by marker or declaration refused without Upstash, undeclared refused, only a declared `single-process` opens the file backend and `spend()` passes the environment through (`ledger-stores.ts:69`); the CLIs declare for themselves only when nothing is declared (`declaredSingleProcess`, `ledger-stores.ts:104`; `run.ts`, `tick.ts`, `aeo.ts`); a route reads `COLLECTOR_TOPOLOGY` from the environment or the repo-root dotenv like the collection flags and never declares (`workspace-access.ts:78`, `load-key.ts`); the suite declares once (`vitest.config.ts:87`); `kvLedgerStores(kv)` is the KV shape over Upstash or the in-memory double; CLAUDE.md §7 documents the variable. (5) `ledgerCapUsd(dataDir, env, ledgers)` reads `ledger.json` on the file backend only (`live-gate.ts:65`); the scan route and the loop pass their ledgers. (6) the unreadable-quota refusal is a fixed sentence; the cause rides on the verdict, logged by the scan route and appended to the tick's refusal line (`live-gate.ts:280`, `scan/route.ts:348`). (7) the 409 read-again path is tested through a Db wrapper that races the identical write inside the same transaction (`session-store.test.ts:66,311`). **(8, human-owned, built on instruction:** migration `0005_workspace_role_claim.sql`: `auth_tenant_context.role`, three-argument `stamp_tenant_context`, `current_workspace_role()`, `set_workspace_jwt` requires the role claim and refuses one that disagrees with `workspace_members` (`:181-182`), `set_workspace` prefers an owner, `ws_put_document` and `ws_resolve_request` refuse unless the stamped role is owner or admin (`:227`, `:256`); `mintWorkspaceToken` requires `role` (`token.ts:32`) and `workspaceAccess` passes it (`workspace-access.ts:104`); `check-deploy.sql` names the new reader and stamp.) Not changed, still for a human: the ceiling refusal as an existence oracle; the CREATEROLE walk; the 0003 definer functions taking their subject as an argument; and the consequence recorded in 0005's header, that a member's session cannot write a first category record, so its preview or scan of an unrecorded domain fails at the record write before any spend. | `services/grader/src/live-gate.ts`, `daily-loop.ts`, `ledger-stores.ts`, `run.ts`, `tick.ts`, `aeo.ts`, `load-key.ts`, `store/file-store.ts`, `apps/public/app/api/gaps/route.ts`, `scan/route.ts`, `apps/public/lib/workspace-access.ts`, `vitest.config.ts`, `packages/db/migrations/0005_workspace_role_claim.sql`, `packages/db/src/token.ts`, `packages/db/scripts/check-deploy.sql` | `session-store.test.ts` (item 1: two workspaces, both flags on, key present, fetch stubbed shut, no foreign host in any scan event, no fetch, no cycle; item 2: Two reads acme.test 200, 200, 429 and One is served; item 7: 409 `read-again`, version 1 stands, read again it applies; item 8: a member's store is refused at the database for the write and the resolve with `applies()` bypassed); `daily-loop.test.ts` (item 3, over `MemoryKV`: a second tick refuses on the lease and the lease is released with nothing on disk; two ticks overlapping through a reclaimed stale lease book against one total, the host in flight is refused, the cap admits one cycle in all; a gate refusal books nothing; the file-backend lease cases); `ledger-stores.test.ts` (item 4: declared fleet refused, undeclared refused, marker beats declaration, `declaredSingleProcess` never overrides a fleet, the KV shape over the double); `workspace-access.test.ts` (a declared fleet is 503); `live-gate.test.ts` (items 5, 6); `workspace-state.test.ts` and `token.test.ts` (item 8: a member's token refused by both writers with no gate in the way, filing and cycles still allowed, an admin applies, an overstated or missing claim refused at verification and nothing stamped, the mechanism assertions) | DONE ⚠️ HUMAN REVIEW | ⚠️ spend control (3, 4, 5): `daily-loop.ts` reserve/settle and the tick lease, `ledger-stores.ts` topology choice and `declaredSingleProcess`, `ledgerCapUsd`; ⚠️ tenancy (8): migration 0005, `token.ts`, `workspace-access.ts` minting. **Reviews of 2026-09-15, fixed in the follow-up commit:** `cost-sentinel` found the KV path sound and one MAJOR, that the file-backed ledger document had lost the `wx` exclusivity of the lock it replaced and let two updates in one process interleave across an await; `fileLedgerDoc` now writes under an exclusive `${file}.lock` with a synchronous critical section and a ten-second TTL (`ledger-doc.ts`), with a biting test (`ledger-stores.test.ts`) and both two-tick scenarios re-run on files (`daily-loop.test.ts`); MINOR, `diagnose.ts` opens its own file `Budget` and calls `ledgerCapUsd` without ledgers (a CLI, left). `tenancy-auditor` found no blocker and item 8 sound at the database; MAJOR 1 fixed, `check-deploy.sql` derives that every definer writer of 0004 state takes its workspace from `ws_required()` and every writer of a decision reads `current_workspace_role()`, two failing cases in `check-deploy.test.ts`; MAJOR 3 fixed, the preview and scan routes refuse a member's request for an unrecorded domain before any throttle, fetch or authoring (`recordsFirst`, `workspace-access.ts`; `session-store.test.ts`), and 0005's header no longer claims the record write alone came before any spend; MINOR 2 stated in 0005's header; MINOR 3 fixed (`migrations.test.ts` cuts after every GRANT); MINOR 4 fixed (the renumbering note says 0006). **NOT changed, an owner decision:** MAJOR 2, the per-domain cycle ceiling is keyed by bare host on the deployment-wide ledger (`domain-ceiling.ts:167,200`), so one workspace's two hand-started cycles of a host exhaust the month for every workspace tracking it and the refusal is a one-bit oracle; MINOR 1, the burst cap's repeat exemption (`live-gate.ts:254`) answers whether a host was scanned today by anyone. Both are the ceiling oracle this row already reserves for a human, and re-keying by workspace changes what ADR-0017 bounds against a shared provider quota. Observation: `bank.note` bakes the authoring host into instance-wide state; no route projects it |
| B3d | 2, 7, 9, 10 | Follow-ups from the oversight pass on B3c (2026-09-15; role claim sound, no leak). Decisions taken by the oversight session, owner may overrule: (1) **Admission is per workspace, spend bounds are deployment-wide.** Re-key the per-domain monthly ceiling by `${workspaceId}:${host}` (`domain-ceiling.ts:167,200`) and amend ADR-0017's bound statement: a host's hand-started cycles per month are per workspace, still under the daily cap and the run allowance; the burst-cap repeat exemption stays keyed by bare host and is recorded as accepted, because it only ever admits, never refuses, on another workspace's activity. (2) **A first category record is a measurement's precondition, not a decision:** `ws_put_document` allows version 1 of `category-record` for any member and keeps the owner/admin check for every other write; `recordsFirst` (`workspace-access.ts:132`) and the two route refusals go; test: a member previews and scans an unrecorded domain, and still cannot apply a correction. (3) `check-deploy.sql:191-206` derivation holes: cover UPDATE/DELETE of `workspace_documents`, an INSERT into `workspace_requests` that lands `status='applied'`, schema-qualified names, and refuse any definer that uses dynamic `EXECUTE` against those tables, for any owner role; a bite test per hole. (4) `ws_file_request` (`0004:220`) lets a member displace another account's pending filing; a member may replace only their own, owner/admin any; test. (5) one test for `set_workspace`'s owner→admin→member ordering (`0005:201-215`). (6) The vitest worker RPC timeout (`Timeout calling "onTaskUpdate"`) reproduces on the full suite with all tests passing; find the cause (the 50 s pilot-analysis test is the suspect) and fix it deterministically so CI's exit code never depends on it. **Done 2026-09-15, items 1 to 6, six commits (`4a2892f`, `3e42885`, `01be726`, `21cb974`, `7ecedbb`, and item 6's).** (1) `ceilingKey` is `${workspaceId}:${host}`; `local` keeps the bare host so a machine's month is unchanged (`domain-ceiling.ts:111`); the scan route passes the session's workspace (`scan/route.ts:339`); the refusal names the workspace's own ceiling; ADR-0017 Amendment 2 (`0017-daily-scheduler.md:467`) amends the bound statement and records the burst-cap repeat exemption as accepted. (2) migration `0006_member_first_record.sql:62`: version 1 of a category-record is any member's write, everything else owner/admin, the version rule checked first; `recordsFirst` and both route refusals removed. (3) `check-deploy.sql:164`: the derivation runs before the allowlist (an owner always holds EXECUTE on its own function), over every definer that touches workspace state whoever owns it, INSERT/UPDATE/DELETE, schema-qualified or quoted, an INSERT into `workspace_requests` naming `status` is a decision, dynamic `EXECUTE` refused outright; six failing databases (`check-deploy.test.ts:300`), each run against the previous SQL first: five were refused for the allowlist's reason, one passed. (4) migration `0007_filing_ownership.sql:32,53`: `filed_by` stamped from the context; a member replaces only its own pending filing, owner/admin any, a pre-0007 row only owner/admin; `StoredRequest.filedBy`; the three routes answer 409 `pending-elsewhere` (`workspace-access.ts:130`). (5) `workspace-state.test.ts:414`: owner over admin over member, nobody raises and stamps nothing, a member-only maintenance session is refused a decision and may write a first record. (6) the cause measured, not guessed: the suite without the pilot test reproduced the timeout and the pilot file alone ran clean; in the failing runs `pre-spend-hook.test.ts` took 71 s of wall time while its 34 synchronous bash spawns summed to 83 s, and the runner yields only microtasks between synchronous tests, so the worker never read the acknowledgement vitest's 60 s worker RPC waits for; now one asynchronous bash runs every case (`pre-spend-hook.test.ts:102,131`), three consecutive clean full runs in an isolated worktree against four failures in six before, no timeout raised. **Found, not fixed, a follow-up row:** `apps/public/app/api/scan/route.test.ts` reads and writes the machine's own `data-live` and expects a pipedrive.com cycle only this machine holds; it failed in every clean-checkout run and would fail in CI. | `services/grader/src/domain-ceiling.ts`, `docs/adr/0017-daily-scheduler.md`, `packages/db/migrations/0006_member_first_record.sql`, `0007_filing_ownership.sql`, `packages/db/scripts/check-deploy.sql`, `packages/db/src/schema.ts`, `services/grader/src/store/pg-store.ts`, `file-store.ts`, `apps/public/lib/workspace-access.ts`, `api/preview/route.ts`, `api/scan/route.ts`, `api/category/route.ts`, `api/competitors/route.ts`, `api/custom-prompts/route.ts`, `services/grader/src/pre-spend-hook.test.ts` | `domain-ceiling.test.ts` (two workspaces on one ledger, the file keys them apart, a machine's pre-split file keeps its month); `session-store.test.ts:416` (Two's two cycles do not refuse One through `/api/scan`, Two is refused on its own), `:316` (a member previews and scans an unrecorded domain, records version 1, is filed not applied for a correction, refused version 2 at the database), `:344` (a second member is answered 409 on all three routes and the first member's rows stand, then replaces its own); `workspace-state.test.ts` (member writes v1 and nothing else that decides; a filing is the filer's, admin/owner replace any, a legacy row is theirs alone; `set_workspace` ordering); `check-deploy.test.ts:300` (six holes, one failing database each) | DONE ⚠️ HUMAN REVIEW | ⚠️ tenancy (2, 3, 4): migrations 0006 and 0007, `check-deploy.sql`'s derivation; ⚠️ spend control (1): `domain-ceiling.ts` and the amendment's bound statement. **Reviews of 2026-09-15, fixed in the follow-up commit.** `cost-sentinel` (item 1): the re-keying is safe (`local` never reaches a fleet, a uuid cannot collide with the bare host, nothing double-books, the loop is untouched); MAJOR, the amendment's bound statement was wrong — a hand-started scan never meets the daily cap, which is the loop's alone; what bounds it for everyone is the burst cap on new domains, the provider's live quota, the per-run allowance and the lifetime ledger, none of which scales with the number of workspaces — corrected in ADR-0017 Amendment 2 and the scan route comment, and **for the owner:** re-keying removed the only bound that made monthly hand-started spend self-limiting per host across the deployment (about $116 a month for a hundred workspaces each taking two cycles of one host, until the lifetime cap binds); an aggregate ceiling would be a spend cap per deployment or plan, the recurring budget the ADR's open section already describes; MINOR, the burst-cap exemption's "cached re-show" holds for the curated cells only, a repeat with a workspace's own custom prompts buys those cells on a day the cap was full, bounded by the ceiling, the allowance and the ledger — the amendment now says so. `tenancy-auditor` (items 2 to 4): no blocker, no query crosses a workspace boundary, 0006 and 0007 are covered by the migrations test's derivation; MAJOR 1 fixed, the derivation missed MERGE, ONLY, TRUNCATE, COPY, an upsert whose ON CONFLICT lands a status, invoker helpers and updatable views — `check-deploy.sql` now scans every function in a non-catalog schema, counts those verbs as writes and an upsert or merge that can land a status as a decision, refuses an updatable view over the three tables, and asserts the tables exist, seven more failing databases in `check-deploy.test.ts`, each shown to fail against the previous SQL first; MINOR 2 fixed, every definer in public must pin `search_path`; MINOR 3 fixed, `ws_put_document`'s exemption is pinned to its text; MINOR 5 fixed, `ws_file_request` takes the workspace row lock before the standing check and answers a unique violation with the same refusal (0007 amended in place: unapplied anywhere, on this branch only); MINOR 4 recorded, not changed: the caller-chosen `requested_at` is the optimistic token of a resolve, unreachable through the routes, reachable from the CLI's own machine — keying the resolve on `id` is the fix when the CLI meets the Postgres store; MINOR 6 recorded, **for the owner:** any member may now start first cycles on any number of new hosts, each with a fresh per-workspace ceiling, bounded only by the burst cap, the visitor throttle, the provider's quota and the lifetime ledger — a per-workspace cap on first records a day is the same spend-cap question as the cost-sentinel's |
| B5 | 10 | `apps/public/app/api/scan/route.test.ts` reads and writes this machine's `services/grader/data-live` and expects a pipedrive.com cycle only this machine holds (`:20-25`, `:118`), so the suite fails on a clean checkout and in CI (B0's gate has never actually passed remotely: the branch was first pushed 2026-09-15). Fix: the test builds its own fixture data dir (a cached cycle written from a fixture result) and never touches `data-live`; a grep test asserts no test file references `data-live`. Verify by the CI run on the branch, not locally. **Done 2026-09-15 (`ebbc36d`):** the scan route test writes a fixture cycle into a fresh `GRADER_DATA_DIR` per test; the preview route test runs over a scratch directory too (it used to write records and its throttle ledger into the live one and restore them); the two guarded tests that read whatever data the machine holds (`scan-result.test.ts`, `promote-competitors.test.ts`) resolve it through `lib/data-dir.ts` and its new twin `services/grader/src/data-dir.ts`; two comments reworded; `build-env.test.ts` asserts no tracked test file names the live directory. Verified in a clean worktree with no live data and no dotenv (131 files, 1797 tests, exit 0) and by CI run 34963093487 on the pushed commit: typecheck and test both green, the branch's first green run | `apps/public/app/api/scan/route.test.ts`, its fixture, `build-env.test.ts` | the workflow run on `mvp/stage-a` is green (run 34963093487, success) | DONE | no |
| B6 | 9 | **Spend decision (oversight, 2026-09-15, owner may overrule):** re-keying the cycle ceiling per workspace (B3d item 1) removed the only per-host bound on hand-started spend across the deployment; a hundred workspaces each taking two cycles of one host is about $116 a month until the lifetime cap binds, and any member may open first cycles on any number of new hosts. **Accepted for the MVP** because the burst cap on new domains per day, the per-visitor throttle, the provider's live quota and the lifetime ledger still bound the day and the total; the real per-workspace bound is entitlement, so D2 must gate hosts per workspace by plan (`ws_put_cycle` and `ws_file_request` refuse a host beyond the plan's tracked-host count, the way 0001 gates `score_rows`) and until D2 lands `GRADER_MAX_NEW_SCANS_PER_DAY` is the operator's lever. Recorded in ADR-0017 Amendment 2 by C1 | `docs/adr/0017-*.md` | — | DECIDED, built by D2 | owner reads |
| B4 | 7 | Corrections become operator actions inside the workspace (category, competitor set, custom prompts), still versioned, never re-derived; CLIs stay. **Done 2026-09-15:** `checkCorrection` + `correctionOf` (resolve-category.ts), `nextOverride` (competitor-overrides.ts), `nextPromptSet` (custom-prompts.ts) are the pure decisions; `correctCategory`, `applyOverride`, `applyCustomPrompts` (the CLIs, files) and `correctCategoryIn`, `applyOverrideIn`, `applyCustomPromptsIn` (the store, written against the version that was read) are their twins. `workspaceAccess` returns the session's `role`; POST on the three routes from an owner or admin on the Postgres store applies (version N+1, history kept, the pending request that asked for exactly this marked applied by the account) and answers `{ applied: true, version, request }`; a member, or the file store, files a request as before. The three client libraries pass `applied` through and the components say which happened. | `session-store.test.ts` (B4 cases: a member files, the owner applies version 2 with version 1 kept and the request marked applied by the owner's account; the same slug again is refused and version 2 stands; a member cannot apply; competitors and prompts likewise, version 1 each, no-change refused; the other account 404s); `file-store.test.ts` (the apply twins agree with the CLI functions, including the override-in-force refusal); the `correctable-context.gate.test.ts` file-path proof is unchanged and still green | DONE ⚠️ HUMAN REVIEW | ⚠️ tenancy: the apply path writes through the session's token only. **Audits of B3b and B4 (2026-09-15), fixed in the follow-up commit:** the ceiling refusal no longer states a count (a deployment-wide ledger's count may be another workspace's); `ws_put_document` refuses a NULL expected version; the four store fallbacks refuse a fleet runtime (`defaultWorkspaceStore`); no result file on the Postgres backend; one exported `applies()`; the per-domain correction caps keyed by workspace; a lost version race is a 409. **Open for a human:** the role is enforced in the app only, the token carries no role claim the definer functions check (B4 audit MAJOR 1, the real fix is human-owned); a CREATEROLE deployer on PG16+ is auto-member of the roles 0000/0001 create and would trip the tenant-role walk and exclusivity (B3b audit MINOR; a REVOKE in 0000/0001 would also remove the ADMIN the later migrations' GRANTs need, so this is a deploy-procedure question); apply and resolve are two transactions. |

## Stage C — recurring operation (point 6)

| # | Goal | Item | Files | Test | Status | Human |
|---|---|---|---|---|---|---|
| C0 | 10, 11 | **Integrate `main` into `mvp/stage-a` before anything else merges.** `main` carries PR #3 and PR #4 (the tenancy deploy gate: `5dd2af5`..`57f1d37`, 13 files in `packages/db`, the GUC-free context, the RLS-bypass runbook) that the branch lacks; `git merge-tree` (2026-09-15) shows six conflicts: `CLAUDE.md`, `packages/db/scripts/check-deploy.sql`, `packages/db/src/check-deploy.test.ts`, `packages/db/src/tenant-isolation.test.ts`, `scripts/crawl.mjs`, `services/grader/src/live-gate.ts`; and `main`'s `0003_tenancy_exposure_manifest.sql` collides with the branch's `0003_accounts_identity.sql` under B3r's number index. Do it in a separate worktree, never in a tree another session is using. For the manifest: decide whether B3r/B3d's derivation already subsumes it (then fold its assertions into the derivation and its tests, and drop the file with the reason in the commit) or renumber it 0008 and reconcile; either way `migrations.test.ts` and the deploy check pass. Then push, confirm CI green, and merge the branch into `main` (a merge commit; sessions push `main`, the owner does not) **Done 2026-09-15, in a separate worktree (`integrate/c0-main`, fast-forwarded onto the branch).** The manifest is **0008, renumbered and reconciled, not folded**: two of its assertions (`assert_role_powers()`, `auth_key_health()`) exist nowhere else and the second must be a definer owned by `auth_verifier`, so a migration was needed on any path; and the manifest is the declared surface plus a derivation over every privilege, relkind, schema and role (the branch's read inverse covers SELECT only), which DO blocks would have lost. Changes at the renumbering, all named in the file's header (`0008_tenancy_exposure_manifest.sql:6-31`): records itself first in one transaction (`:74-78`, cut-after-GRANT proven by `migrations.test.ts`); the 0004 state tables declared, scoped SELECT and `svc_onboard` service writes (`:133-135`, `:166-171`); the definer owner rule names `auth_verifier` and `svc_onboard`, never the scorer (`:577`); the tenant-callable list is the branch's twelve (`:603-610`); `migration_record()` (`:715`) because the deploy principal main prescribes got `permission denied` on the record and could not run the gate; and from the audit, a `service` row names its **grantee** and the sweep matches on it (`:108-112`, `:241`), closing the audit's MAJOR. `check-deploy.sql` runs main's order and keeps every derivation the branch had (`scripts/check-deploy.sql:73` the manifest; `:89` the FORCE sweep; `:244` the definer-body rules; `:333` the declared list, duplicated with the manifest's by decision; `:397` the record through the definer, refused when empty; `:450` the read inverse; the partial-gate footer gone). Tests: `only`/`refusedBy` run one gate section alone where the manifest speaks first (`check-deploy.test.ts:59-69`, `:116-121`), so each derivation is proven by its own message; main's 77 cases re-run over the full lineage (`deploy-check.test.ts`, now 79 with the grantee and owner cases at `:562`, `:579`); one expectation moved and recorded (a declared invoker view: manifest accepts, read inverse refuses, `:844`); `tenant-isolation.test.ts:204-244` adds the manifest coverage test and the seam (reachability = manifest scoped ∪ shared, pg_policies = manifest scoped). `live-gate.ts` keeps the branch's derived default (main's 10 was marked temporary); `crawl.mjs` takes main's two-stage harvest over the branch's selectors; CLAUDE.md §9 records the closure as merged. Local gate: typecheck and 134 files / 1924 tests green before the audit fixes, re-run after them. **Merged into `main` with a merge commit by this session; CI run ids in the Status update at the session's end** | the six files, `packages/db/migrations/0008_tenancy_exposure_manifest.sql` (renamed from main's 0003), `packages/db/src/deploy-check.test.ts` (from main, adapted), `docs/runbooks/deploying-the-database.md` (from main), `docs/adr/0007-*.md` (integration note), `docs/MVP_REVIEWS.md` | full gate locally and CI green on the branch and on `main` after the merge | DONE ⚠️ HUMAN REVIEW | ⚠️ tenancy: migration 0008 (renumbering, rows, grantee column, the two rule changes, `migration_record()`), `check-deploy.sql`'s merged order, the invoker-view refusal. Audit recorded in MVP_REVIEWS.md row C0; follow-ups for the tenancy owner: a grantee beside every declared definer (a PUBLIC grant on `ws_put_document` passes today), one source for the definer list and the shared allowlist, a rule for `security_invoker` views |
| C1 | 6 | D4: ADR-0018 — QStash daily schedule fans out one signed job per tracked domain into `runTick`'s per-domain path under the existing daily cap and allowance; cite Vercel function duration limits and QStash schedule semantics from provider docs. **Done 2026-09-15:** ADR-0018 (Proposed). The structural fact: one invocation cannot run the day (Vercel Hobby 300 s maximum, Pro 800 s, streamed responses counted; `/docs/functions/limitations` 2026-08-24), so the tick is two shapes of signed job on one route, `/api/tick`: a scheduled `fan-out` that, under the tick lease, reads the tracked list, builds the due list per workspace, books the day's cap and a fan-out mark in the daily ledger and publishes one `domain` job per due domain with a deduplication id; and a `domain` job that runs the loop's per-domain path (reserve, gate, allowance, collect, file, settle) for one host in one workspace, refusing any host already booked today in any status, a day that is not today, and a day the fan-out did not book, and answering 200 with the outcome for anything that may have spent (the R3 shape). Verification before anything (issuer, subject URL, exp, nbf, body hash, both keys; QStash `/docs/qstash/features/security`, `/howto/signature`, `/howto/roll-signing-keys`, `/features/retry`, `/features/deduplication`, `/api-reference/schedules/*`, fetched 2026-09-15). The tracked list is the deployment's `tracked.json` ledger document (the machine's file, or the Upstash document); a job opens the workspace through a token minted for the account that switched the domain on, re-verified against membership by the database (⚠️ tenancy). Mode from the environment only: `armed` live, `fixture` with identity off only. Registration and arming are two acts, the owner's. States that a loop job runs under the daily cap, the tick lease and the daily-ledger fold while a hand-started cycle does not; carries the per-workspace ceiling and the B6 decision as the addendum to ADR-0017 Amendment 2. Names C3 (tracking on the deployment) as the write path C2 does not build | `docs/adr/0018-qstash-daily-fanout.md`, `docs/adr/0017-daily-scheduler.md` (Amendment 2 addendum) | — | DONE | owner reads; ⚠️ tenancy (D6, the token minted for the tracking account) |
| C2 | 6 | Registration (`pnpm grader:schedule -- --register`) using the existing `QStashClient.schedule(cron, body)` (`services/collector/src/qstash.ts:141`, hand-rolled fetch, no `@upstash/qstash` dependency); arming path = env `GRADER_DAILY_LOOP=armed` on the deploy + schedule exists; **never armed, never spent by a session**. **Built 2026-09-15:** `QStashClient.publishJson` (deduplication id, delay, destination timeout), `schedule(cron, body, { scheduleId, retries, timeoutSec })`, `listSchedules`/`pauseSchedule`/`resumeSchedule`/`deleteSchedule`; `due.ts` split into the pure `decideDue` (`due.ts:216`) with the file twin `dueToday` and the store twin `dueTodayIn` (`:332`), `readTrackedIn` over the `tracked.json` ledger document (`:115`, the machine's file or the Upstash document), `decideDueIn` for a job's re-decision (`:351`); `daily-loop.ts`: `liveGates` shared by the tick and both jobs (`:485`), `runAdmitted` the one per-domain path (`:541`), `reserveJob` refusing a line already booked today in any status and a day the fan-out did not book (`:276`), `openDay` before publishing and `closeDay` after (`:332`, `:347`), `runFanOut` (`:738`) and `runDomainJob` (`:819`), the day's lines keyed `${workspaceId}:${host}` (bare host on a machine) so two workspaces on one host do not collide; `apps/public/lib/tick.ts`: `handleTick` verifies over the raw bytes first (`:127`), then the shape (`:138`), the mode from the environment only (`:142`), fixture refused with identity on (`:145`), `jobStoreFor` minting the tracking account's token and taking the database's verdict per entry (`:85`); the route is the transport (`maxDuration = 300`); `pnpm grader:schedule` prints by default and needs `QSTASH_TOKEN` from the shell for every other action (`schedule.ts:75`, `:122`, `:129`); the pre-spend hook blocks `--register` and `--resume` (`pre-spend.sh:46-47`). Nothing registered, nothing armed, no credential anywhere; the deployment's tracked list has no write path until C3, so a fan-out there publishes nothing | `services/collector/src/qstash.ts`, `index.ts`; `services/grader/src/due.ts`, `daily-loop.ts`, `schedule.ts` (new); `apps/public/lib/tick.ts` (new), `app/api/tick/route.ts` (new); `.claude/hooks/pre-spend.sh`; `package.json`; `CLAUDE.md` §7, §9 | `schedule.test.ts` (the spec from the environment; refusals; every action but print refuses without the token and calls nothing; the documented requests); `qstash.test.ts` (+3); `due.test.ts` (the ledger document on files and KV, `dueTodayIn` equal to `dueToday`, `decideDueIn` over time); `daily-loop.test.ts` (the two jobs over the KV double and files: the day opened before publishing and closed after, a second fan-out publishes nothing, a job needs the fan-out's day and today, reserves under the STORED cap, files and settles, its retry runs nothing, a second host beyond the cap is refused, a thrown collector is settled and its retry already-booked, a died fan-out is re-run, not-armed before the lease, a refused entry reported, lease-held, two workspaces on one host each get their line); `api/tick/route.test.ts` (503 unconfigured, 401 unsigned/other key/other URL/other body, the next key, 400 shapes, not-armed does nothing; identity off + fixture through the real route and runner: fan-out publishes with the deduplication id and 300 s timeout after opening the day, the domain job files a cycle marked fixture and settles, retries do nothing, no-fan-out/day-passed/not-tracked, 503 without the token, lease-held, corrupt ledger; identity on + armed over PGlite and the KV double with the runner and quota gate mocked: fixture refused, the fan-out publishes per workspace and refuses the non-member entry at the database, the domain job files in its entry's workspace and no other, a forged workspace is not-tracked, the retry runs nothing, disarmed does nothing); `pre-spend-hook.test.ts` (+7) | DONE ⚠️ HUMAN REVIEW | ⚠️ spend control: `daily-loop.ts` (`reserveJob`, `openDay`/`closeDay`, `liveGates`, `runAdmitted`, the line key), `pre-spend.sh`, `schedule.ts`; ⚠️ tenancy: `lib/tick.ts` `jobStoreFor`; owner registers and arms. **Reviews of 2026-09-15 (both fixed in the same commit; two MAJORs and four items recorded for the owner).** `cost-sentinel`: no path found where a QStash retry re-enters collection after a spend (every non-2xx of the route is before a reservation; a thrown collector is settled before the answer; concurrent jobs fold under the document lock; a job killed at 300 s leaves a `running` line a retry meets; a fan-out that died between opening and closing the day republishes safely); fixture mode cannot reach a provider (the adapter is chosen by mode, offline price is zero). Two findings, both fixed: the fan-out mark now records `failed` publishes so a day QStash under-took is in the ledger (`daily-loop.ts` `closeDay`, tested), and the ADR's message headroom is corrected — with two retries a day can cost three messages per domain, so the free plan's 1,000 a day is about 300 tracked domains, not 900. Recorded, not changed: the hook's new block is bypassed by the variable-splitting class the hook's own header discloses (`F=--register; …`), and the control that holds is `QSTASH_TOKEN`'s absence from every session's environment (`.claude/settings.json` injects only `COLLECTION_ENABLED`); **for the owner:** confirm no shell profile, CI secret or container default could put `QSTASH_TOKEN` into an agent session's inherited environment. `tenancy-auditor`: no BLOCKER; the path from the signed bytes to `ws_put_cycle` never reaches a workspace other than the one the tracked entry names (the body is a lookup key only, the store is built from the entry, the database re-verifies membership and role on every presentation); no tenant-facing route reads the tracked list or the daily ledger; the unauthenticated answers are fixed sentences; nothing for `check-deploy.sql`. Found and fixed during the review: the day's ledger line was keyed by bare host, so two workspaces on one host collided (now `${workspaceId}:${host}`, tested). Six MINORs fixed in the follow-up: the job token lives 600 s, not a session's hour (`lib/tick.ts` `JOB_TOKEN_TTL_SEC`); an entry whose `by` is not an account id is refused before minting (the parser's `operator` default); the result file follows the STORE's shape (`resultFile`, only a file store), not the ledgers'; the fan-out's answer to QStash carries counts only and the hosts go to the server log; the publish order rotates with the day (`rotateByDay`); and the four named test gaps are closed (a member's entry files a cycle, no-role and non-account entries refused, `already-booked` at the route under the workspace key, a role changed after tracking refused at the database). **Two MAJORs recorded for the owner, not changed:** (1) fairness — the cap is one deployment-wide figure admitted first-come, so under a binding `COLLECTION_BUDGET_USD_DAILY` a small workspace behind a large one can be refused every day and nothing tells it; rotation is the interim, a per-workspace share of the cap is entitlement (D2); (2) the tracked document is the record that authorises cross-workspace collection and lives outside the database, unscoped and outside every standing gate — harmless while nothing on the deployment writes it, and C3 must derive `workspaceId`, `by` and `role` from `workspaceAccess()` only (never a body), or move the list into a workspace-scoped table, which is also the first step of a loop service identity (a `loop` claim over a grant table, its own signing key, an actor column on `workspace_cycles`). **Recorded, not changed:** the burst cap (`GRADER_MAX_NEW_SCANS_PER_DAY`, default 12) is keyed by bare host and counts every tracked host as new each day, so a tracked set wider than the cap has jobs refused `burst-cap` daily — the owner sets the cap above the tracked set when arming, or decides to exempt loop jobs (their bound is the daily cap); a role change after tracking stops that domain silently (fail-closed; the service identity is the fix); a loop-filed cycle records no actor (migration, human-owned); the KV ledger lock is a TTL lease without fencing (B3c, human-owned), which C2 makes matter unattended |
| C2r | 6, 9 | Oversight cost review of C2 (2026-09-15, independent; verdict NOT SAFE TO ARM until this lands). (1) **MUST:** `kvLedgerDoc.update` (`services/grader/src/ledger-doc.ts:128-147`) writes the document with an unconditional `set` after a `setnx` lease; a holder stalled past the 10 s TTL overwrites a newer holder's write, which erases a `running` reservation, and a QStash retry in that window is admitted again by `reserveJob` and collects a second time. Fence the write: the document `set` and the lock-ownership check are ONE atomic operation on the store (the same script mechanism `delIfEquals` uses), a stale holder's write is refused and surfaces as a thrown error, never a silent success; a test with a deliberately stalled holder proves the stale write is refused and the reservation survives, on the in-memory double and with the real command shape pinned. (2) The per-domain allowance is computed at the mean engine price (`daily-loop.ts:580-590`), so the day can exceed `COLLECTION_BUDGET_USD_DAILY` by up to $0.02 × domains run. Decision: compute it at the dearest engine price so the ceiling is hard; accept that the last domain of a day may get less retry headroom, and say so in the log line. (3) A publish failure past the QStash plan is booked as `failed` on the day's mark and read by nothing: the dry-run tick CLI prints today's and yesterday's `fanOut.failed` when non-zero, and the fan-out's own log line names the count. (4) From the builder's tenancy review, small: a `by` that is not a uuid on a deployment is refused at `tick.ts:96` as "no account to run as" rather than minted; `outFile` at `daily-loop.ts:899` keys on the store backend like `scan/route.ts:401`, not the ledger backend. **Decided, recorded, not built here:** loop jobs bypass the new-domain burst cap (they are bounded by the daily cap, the per-run allowance and, from D2, the tracked-host entitlement); hand-started scans keep it; ADR-0018 records it. The fairness of one deployment-wide cap is rotation now (built in C2) and a per-workspace share under D2 | `services/grader/src/ledger-doc.ts`, `ledger-doc.test.ts`, `daily-loop.ts`, `daily-loop.test.ts`, `apps/public/lib/tick.ts`, `tick.ts` CLI, `docs/adr/0018-*.md` | listed per item; `pnpm vitest run services/grader apps/public/app/api/tick` green; CI green | TODO | ⚠️ spend control |
| C3 | 6, 2 | Tracking becomes a workspace action on the deployment (ADR-0018 D6): a `POST /api/tracked` from an owner or admin writes the entry `{ host, workspaceId, by, role, since, reason }` into the deployment's `tracked.json` ledger document after checking the record exists in the workspace's store, and removes it on `off`; a toggle beside the new-cycle panel; `pnpm grader:track` stays the machine's CLI and writes `workspaceId: local`. Until it lands a fan-out on the deployment finds nobody tracked and publishes nothing. Later D2 gates the count of tracked hosts per workspace by plan (B6). **From the C2 tenancy review (MAJOR-2):** the route derives `workspaceId`, `by` and `role` from `workspaceAccess()` and from nothing in the body (ADR-0002 Amendment 2's rule), or the list moves into a workspace-scoped table so the standing gates and `tenant-isolation.test.ts` cover it — the first step of a loop service identity; and it re-reads the role from the session at every write so a promotion does not strand the entry | `apps/public/app/api/tracked/route.ts` (new), `services/grader/src/due.ts` (`setTrackedIn`), `apps/public/components/new-cycle.tsx` | route test over PGlite and the KV double: a member is refused, an owner's entry is read back by the fan-out's due list, a host with no record is refused, another workspace's entry is invisible | TODO | ⚠️ spend control (a tracked host is a daily spend); owner arms |

## Stage D — personas and sale (points 1, 2, 4, 5)

| # | Goal | Item | Files | Test | Status | Human |
|---|---|---|---|---|---|---|
| D1 | 2 | Agency portfolio from the account's real workspaces; delete `agency-fixture.ts` (`CLIENTS` L62–72, `PORTFOLIO` L74–83) and the `PORTFOLIO.map` block in `agency/page.tsx` L276–336; `readAgencyDomains` (localStorage, `workspace.ts:299`) replaced by the account's workspaces | `apps/public/app/agency/page.tsx`, `lib/agency-fixture.ts` (deleted), `lib/workspace.ts`, `agency/page.test.tsx` | `planned.test.ts`/render tests: no fixture name reaches markup | TODO | no |
| D2 | 1 | D5: Stripe Checkout + webhook writes `workspace_subscriptions` (the entitlement trigger from migration 0001 already refuses unpaid brands). Also: `workspace_cycles` and `workspace_requests` accept any host today (oversight review 2026-09-10, LOW-2), so the plan's tracked-host limit must gate `ws_put_cycle` and `ws_file_request` the way 0001 gates `score_rows` | `apps/public/app/api/billing/checkout/route.ts`, `api/billing/webhook/route.ts`, `packages/db` | webhook test with a signed fixture event; RLS/trigger test: brand insert refused before the row, allowed after | TODO | ⚠️ tenancy |
| D3 | 5 | `/api/answers` served from R2 for any workspace (after B3) | `apps/public/app/api/answers/route.ts`, `services/grader/src/answers.ts` | route test against an in-memory R2 double | TODO | no |
| D4 | 4, 5 | Purpose-built visuals for the three table-only insights: per-prompt grid, cited sources, gap coverage | `apps/public/components/prompt-grid.tsx`, `cited-sources.tsx`, `gap-report.tsx` | render tests; `depth.test.ts` still green; stats-reviewer on any rendered metric | TODO | ⚠️ stats-reviewer |

## Stage E — measurement (points 1, 3, 9)

| # | Goal | Item | Files | Test | Status | Human |
|---|---|---|---|---|---|---|
| E1 | 3, 9 | D6: curated bank `leaders` become a detection dictionary only (still fed to `scoreAnswer` as `competitors` for detection); the ranked/position set comes from `promoted-competitors/<slug>.json` (`promote-competitors.ts:531`) and the head-to-head reads "not yet established" when none exists; `/score-version` bump det-3 → det-4 | `services/grader/src/scan.ts` (`leadersOf` L314, competitor set L577), `services/scorer/src/score.ts` (position L378–381), `packages/taxonomy/src/types.ts`, `docs/METHODOLOGY.md` changelog (L119–129), both pin tables | `pnpm grader:version-diff` flip list; scorer golden agreement; render test for the "not yet established" state | TODO | ⚠️ scoring rule set |
| E2 | 3 | D7a: the 100-domain G3 worklist (`services/grader/labels/worklist.json`, unlabelled) | `label-run.ts` | worklist has 100 distinct real domains | BLOCKED (owner labels) | owner |
| E3 | 1, 9 | D7b: costed G0 pilot plan per PHASES 0.5 using `collector:pilot` | `docs/runbooks/g0-pilot.md` | — | BLOCKED (owner go-ahead; spends) | owner |

---

## Verification (Stage A, this session)

1. `pnpm test` and `pnpm typecheck` green after every row; each row its own commit.
2. Hook test exercises the real `pre-spend.sh` through bash+jq (both present on this machine).
3. `cost-sentinel` agent on the A2/A3 diff; `stats-reviewer` on A7's formatter.
4. Report per goal point with file:line at session end, as instructed.
