# Reviews of the ⚠️ HUMAN REVIEW rows — the delegated record

> On 2026-09-15 the owner delegated the review of the flagged rows (migrations,
> spend control, the stats formatter) to the oversight session, and asked that
> pushes, merges and other command-line steps be handled by sessions, not by
> hand. This file is the auditable record of that delegation: what was reviewed,
> by which independent pass, what it found, and the verdict. CLAUDE.md §4 still
> names these areas human-owned; the human's act here is the delegation itself,
> recorded once, and every later verdict cites the evidence a person could
> re-check. Rows keep the ⚠️ marker in MVP_PLAN.md until this file says ACCEPTED.

Method for every row: the builder ran its own audit; the oversight session then
ran an independent second pass (tenancy-auditor, cost-sentinel, or
stats-reviewer) that was told not to trust the builder's notes, and confirmed
the material findings first-hand at the cited lines. Where the second pass
found something the first missed, that became a new row, was built, and was
passed again.

| Row | Area | Independent pass | Material findings of the pass | Verdict |
|---|---|---|---|---|
| A2, A3 | spend control | cost-sentinel 2026-09-09 | hook is text matching, ledgers are the bound; accepted as stated in the hook header | ACCEPTED |
| A7 | packages/stats (`formatBounds`) | stats-reviewer 2026-09-09 | bounds round to nearest while `formatFrequency` rounds outward; an interior bound can print as 0.0 from n≈600 | ACCEPTED with one follow-up: adopt outward rounding for bounds (low down, high up) so a printed interval never excludes the true one — row D4 carries it, since it renders metrics |
| B2 | tenancy: 0003 accounts identity, client | tenancy-auditor 2026-09-10 | no leak; 0003 not transactional; `ws_required()` granted to the tenant; the two gates could not see a `USING (true)` table; 0003 numbering clash | ACCEPTED after B3r fixed all four (verified: `0003:71,275`, `0004:255`, `tenant-isolation.test.ts:144-174`, `schema_migrations`) |
| B3a | tenancy: 0004 workspace state, pg store | same pass | store reads name the workspace; writers take the workspace from context; R2/Upstash keys carry no tenant data | ACCEPTED |
| B3r | tenancy: the three fixes | tenancy-auditor 2026-09-15 (as part of the B3b pass) | one MAJOR: the inverse named `app_rw` literally and missed the login role that is a member of it; fixed in 10194fe | ACCEPTED |
| B3b | tenancy + spend: routes through the session token, ledgers off disk | tenancy-auditor + cost-sentinel 2026-09-15 | HIGH: burst-cap refusal listed other tenants' domains; MEDIUM: gaps cap keyed by bare host; spend: tick lock local-disk and daily-ledger write stale on KV; fleet detection marker-only | ACCEPTED after B3c fixed every item (verified: `live-gate.ts:263`, `gaps/route.ts:101`, `daily-loop.ts:164`, `ledger-stores.ts:69`) |
| B4 | tenancy: apply path | same pass | version N+1 checked in the database, 409 on a lost race, no re-derivation; role enforced in the app only | ACCEPTED after B3c item 8 moved the role check into the database |
| B3c | tenancy: 0005 role claim; spend: lease and fold | tenancy-auditor 2026-09-15 (0005 pass) | no leak: mismatched, stale and missing claims refused at verification, stamped role from the membership row; product consequence: a member could not scan an unrecorded domain; derivation holes; member could displace another's filing | ACCEPTED after B3d decided the member's first record and closed the rest |
| B3d | tenancy: 0006, 0007, derivation; spend: per-workspace ceiling | builder's two passes, oversight verification of every cited line, CI green remotely on 57d93c9 | none open; the spend consequence is row B6 (decided) | ACCEPTED |

| C0 | tenancy: the merge of main's deploy gate (manifest → 0008), `check-deploy.sql`, the isolation and gate tests | tenancy-auditor 2026-09-15 (independent, on the resolved worktree before the commit) | no BLOCKER. **One MAJOR, fixed before the commit:** a `service` manifest row matched any of the three trusted roles, so `GRANT INSERT ON workspace_documents TO svc_scorer` plus a matching policy passed the gate and the scorer wrote another workspace's category record (measured; inherited from main's rows on the score tables, extended by the merge onto the state tables); now a row names its grantee and the sweep matches on it, two failing cases (`deploy-check.test.ts:562`). MINORs: the owner rule admitted the scorer with no failing case (fixed: `auth_verifier`/`svc_onboard` only, case at `:579`); the DELETE row cited 0007 for a 0004 privilege (fixed); the stated reason for `migration_record()` was wrong (a non-owner got permission denied, not zero rows; fixed in three places); the twelve-name definer list is grantee-blind (a PUBLIC grant on a declared door passes; follow-up); the shared allowlist exists in four places and only two are bound by the seam, drift fails closed (follow-up: read it from the manifest); the duplicated definer list is a maintenance risk, not a correctness one (follow-up: seed the twelve as rows). Verified first-hand: 0008 is main's file plus exactly the header's changes; every branch derivation survives; no tenant path reaches the record; 267/267 in `packages/db`. Verdict on the decision: renumber-and-reconcile was right, decided by the record's mechanism | ACCEPTED with the three follow-ups recorded; ⚠️ stays until the owner reads 0008 |
| C2 | spend control: the fan-out, the tick route, registration, arming | cost-sentinel 2026-09-15 (independent), after the builder's own two passes | arming conditions, retry ordering, fan-out idempotency and token handling verified clean; **one MUST-fix: `kvLedgerDoc.update` (`ledger-doc.ts:141`) writes the document unconditionally after a `setnx` lease, so a holder stalled past 10 s overwrites a newer reservation and a retry in that window collects a second time**; the per-domain allowance at the mean price lets the day exceed the hard cap by up to $0.02 × domains run; a publish failure past the QStash plan is booked on the mark but surfaces nowhere an operator reads | ACCEPTED on re-verification 2026-09-16: C2r landed the four items and the independent cost-sentinel pass of C2r (row below) moved the verdict to "safe to arm once the owner registers and arms"; ⚠️ stays until the owner reads `ledger-doc.ts` and `daily-loop.ts` |
| C2r | spend control: the fenced ledger write, the dearest-price allowance, failed publishes surfaced | cost-sentinel 2026-09-16 (independent, on the uncommitted change set; wrote and ran a scratch test forcing a settle refusal after a real collect) | no BLOCKER. **One MAJOR, fixed before the commit:** a settle the ledger refused after the collect threw out of the per-domain path; the route answered 503 "nothing was collected" when a collect had run and may have spent; the retry was safe only by the store's `cycle-today` guard, which nothing named. Now: the settle is retried under a fresh lease, and refused twice the run is answered `ran` with `unsettled`, the reservation standing at the expected cost; two independent guards proven alone (`daily-loop.test.ts:628`). MINORs: the CLI's tick had no catch for a ledger throw (fixed, one-line refusal); a stuck `running` line was surfaced nowhere (fixed, the dry listing names one older than a quarter of an hour). NOTEs: the fenced write is correct on all three KV implementations and in every ordering; the dearest price is the correct hard per-domain bound and `remaining` is not double counted; the CLI's ledger read never writes and `declaredSingleProcess` cannot turn a declared fleet into a file backend; under concurrent domain jobs a small per-domain slack remains (recorded in ADR-0018, not a regression); leaving the burst-cap bypass unbuilt can only under-collect, never overspend. Verdict: safe to arm once the owner registers the schedule and arms the deployment | ACCEPTED; ⚠️ stays until the owner reads the spend-control files |
| C3 | METHODOLOGY: ADR-0016 Amendment 1 (the person's edited set is the measurement); spend control: the tracked list's write path, the interim ceiling, the cycle sized on the set; tenancy: migration 0009, `api/tracked/route.ts`, `setTrackedIn` | **Builder-invoked independent passes, on the uncommitted change sets, each told not to trust the builder's notes.** First build (tracking as a workspace action, 2026-09-16): cost-sentinel, tenancy-auditor. After the owner's rescope (the entry flow, 2026-09-16 to 2026-09-19): cost-sentinel, stats-reviewer, and a tenancy-auditor re-check of its own earlier findings. **The oversight session's own second pass has not run.** | **First build.** cost-sentinel: one MAJOR, fixed: the per-workspace ceiling was read before the locked write, so two concurrent switches could both pass it; now decided inside the write's own lock (`due.ts:199`; two concurrent switches at `tracked/route.test.ts:226`). tenancy-auditor: no cross-tenant read leak; one BLOCKER, fixed: the route module exported constants, which fails `next build`'s route type guard while `pnpm typecheck` stays green (moved to `lib/tracked.ts`; `build-env.test.ts` counts the handlers); MAJOR, fixed: with identity off the POST was an unthrottled write that enrols a host into recurring collection (per-visitor and per-domain throttles, booked on success only, `route.ts:131,136`); MAJOR, fixed: one workspace's write re-parsed every other workspace's entries and dropped fields it did not know (foreign entries now pass through verbatim, `due.ts:192`; proven at `tracked/route.test.ts:265`, the unknown field, the malformed entry and the bare string all surviving); MINORs fixed: a switch-off that changes nothing writes nothing; `packages/db`'s own suite grew for 0009 (`workspace-state.test.ts:167`); the file store holds the database's same-day rule (`file-store.ts:129`). **Rescope.** stats-reviewer: **one BLOCKER, fixed before the commit:** the runner read any pinned set version as the headline's set, so a re-score of a cycle stored while decision 4 stood would have republished it, over the same file, as a measurement of a different sample (silent rebasing, R5); the re-score now reads the ROLE the set played off the stored file and pins it (`run.ts:118,484`, `rescore.ts:245`), and the gate test re-derives an old-shape cycle under both roles and shows the difference (`correctable-context.gate.test.ts:171-200`). Five MAJOR, all fixed: the per-day list marked a change of questions but not a scoring-version or collection-path boundary (now every reason `compare()` refuses for, render test `:186`); the record stated the set version at the moment of switching on, not the one the next cycle will ask (now both, apart; `entry-flow.test.ts:149`); the "your K prompts" label vanished at zero mentions, where the lede yields (now also in the basis note, render test `:201`); nothing beside the number said what it may NOT be compared with (`PROMPT_SET_SCOPE`, `lib/prompt-set.ts:38`, and the head-to-head heading, `:215`); no warning that a small set buys a page of refusals (a warning against `MIN_N_FOR_COMPARISON`, never a refusal, `prompt-preview.tsx:108`, `:96`). MINORs fixed: the amendment now states that a person's set may be less independent than the bank's and that no DEFF correction is applied; three sentences that named the bank where a set is asked; a singular for one answer. NOTE recorded in the ADR: decision 4's block basis and a headline set's basis are the same string, told apart only by the field they live in. The Wilson package is untouched. cost-sentinel: no BLOCKER, no MAJOR; every surface that sizes a cycle agrees on the set's cell count (the due list, the loop's allowance, the scan route's gate and allowance, the CLI's bill, the re-score pre-flight), the set REPLACES the bank's cells, an `expired` host costs the day's cap nothing, and saving a set opens no spend path; two MINORs fixed: a category correction's stated next-cycle cost ignored a set in force (`category-requests.ts:245`), and a set saved between the day's decision and the run could change the cycle in flight (the run is pinned to the version its cost was sized on, `daily-loop.ts:971`, `daily-loop.test.ts:388`). tenancy-auditor re-check: every earlier BLOCKER and MAJOR CLOSED, each verified first-hand, "no cross-workspace leak exists in this change set", "committable on tenancy grounds"; its new MINORs fixed: a non-numeric limit switched a throttle off (`positiveIntOr`, `lib/tracked.ts:75`, `route.test.ts:255`); three lapsed instructions blocked a workspace whose status said nothing was tracked (the ceiling counts LIVE entries, `route.test.ts:243`); the domain job did not re-check `until` (`due.ts:332`, `daily-loop.ts:915`, `lib/tick.ts:204`; `tick/route.test.ts:251`); the editor did not say whether a save was applied or filed. **Open, for a person:** the floor on a set's size and the near-duplicate question (METHODOLOGY); `docs/METHODOLOGY.md` has no section for a person's own set; the file store replaces a day's file on a scoring-version bump where Postgres keeps both rows, pre-existing and now asserted as it is (`file-store.test.ts:43`); the operator's CLI is not held to the route's interim ceiling; the tracked document still lives outside the database, where no standing tenancy gate sees it (the first tenancy pass's note to the owner, unchanged) | NOT YET ACCEPTED. The builder's passes are recorded; the oversight session's independent pass and the owner's METHODOLOGY decision are outstanding; every ⚠️ stays |
| C3 (oversight) | METHODOLOGY, spend control, tenancy, and the gate itself | stats-reviewer, cost-sentinel and tenancy-auditor 2026-09-19, independent of the builder's five passes; the oversight session's own full gate on the committed HEAD | **The gate was wrong, not only the code:** C3 landed with typecheck, the suite and CI green and the app's production build broken (`lib/prompt-set.ts`, imported by the client page, named the contracts package root, which carries `node:crypto`). Fixed in `b1ea791`: the browser-safe subpath, CI now builds the app, and a test refuses the package root in browser-side code (proven to fail on the bad import). Statistics: sound with conditions, no BLOCKER; the builder's re-score fix is real and the role is pinned from the stored file; no scoring rule moved, so no version bump; three MAJORs (the basis does not identify the sample across records; the per-day list misses `insufficient-data`; the agency row carries no own-set label) and eight MINORs, the material one being no zero-mention sentence on the record. Cost: no regression; K is sized from the domain's own set end to end; the first cycle meets every gate a hand-started scan meets; the CLI is not held to the tracked-host ceiling; an edit on a day whose cycle ran is served the stored cycle and never says so. Tenancy: no leak; 0009 is transactional, records itself, keeps DEFINER, pin, owner and grants, and `source` authorises nothing; with identity ON the route reads nothing from the request that names a workspace, account or role. Two structural MAJORs belong to the post-MVP identity wiring (the tracked list as one shared document; one enforcement point for "a member may not track"); one bears on the MVP posture (identity OFF means unauthenticated writes define a recurring spend, bounded by binding the server to loopback). Found by the oversight session itself: on the file store a same-day write under a different scoring version overwrites the day's file with no superseded copy (R5), where Postgres keeps both rows | ACCEPTED WITH CONDITIONS: row C3r (fourteen items) lands before P1; the post-MVP tenancy items are recorded in C3r. ADR-0016 Amendment 1 is accepted as the owner's own decision of 2026-09-16 |
| D4 | rendered metrics and the client standard: the question-by-engine matrix, cited sources, gap coverage (branch `mvp/track-d4`, built in a parallel worktree) | stats-reviewer 2026-09-19 (independent; the track's builder has no reviewer of its own, so this pass is the review), then first-hand verification of every fix | First verdict NOT SOUND. **BLOCKER (R8): the cited-sources reach sentence printed "in about 1 in 3 answers" and discarded its bounds, and its test re-implemented the component's branching so it could not catch that.** MAJORs: the matrix sentence said "never name X on any engine" in the present tense, including about an engine that was never asked; one cell per engine silently dropped extra runs; the per-row fraction was unlabelled at simple depth; the top cited class was bolded by point estimate with overlapping intervals; two samples in one sentence (`hostCount` over headline plus custom answers). Nine MINORs (a 0% for a class with citations, a mean of ratios spoken as a share of words, a "least covered" named at perfect coverage, ordering said only to screen readers, rank by hue alone at 1.16:1, engine abbreviations with no key, a top-12 list not called one, reach's denominator unnamed, the gap report's basis unprinted). All fixed in `f4a8bcb`, `f18d01d`, `ac0a181`, each with a test that fails without it; verified at the lines: bounds with the headline's collides guard (`cited-sources.tsx:355-373`), naming gated on the candidate's low bound clearing every other class's high bound else fixed order (`:128-146`), past-tense collected-answer wording with the no-answer engine note (`prompt-grid.tsx:422-434`), cells aggregated across runs (`:345`), the fraction headed "Named in" (`:128`), the rank printed as a numeral (`:151`), one sample for the site count (`cited-sources.tsx:97`). No forbidden file touched; merge into `mvp/stage-a` simulated clean; CI green on `ac0a181` including the app build | ACCEPTED; merged by the main session at its next row boundary |
| C3r (oversight) | METHODOLOGY and rendered metrics: the fingerprinted basis and thirteen follow-ups | stats-reviewer 2026-09-19 (independent, on `ac3e9ea` and `ad243ed`) | **Sound with conditions, no BLOCKER; all fourteen items are real in the code.** The fingerprint is SHA-256 over the normalised, sorted list with the cache key's own normaliser (R6), repeats kept, 48 bits, written and parsed only in `basis.ts`, no consumer compares by prefix; a revert to an identical set compares as one sample through `sameBasis` while every other `compare()` guard still fires; a cycle without the tail parses and is never equal to one with it. The zero sentence's bound is the Wilson upper bound through the formatter (0.434 at n=5, checked against the statsmodels reference). R5 on the file store: the superseded file is copied before the write and a failed copy fails closed. One MAJOR: the edit-on-a-day-already-run message landed on the Grader only, not on the record or the new-cycle refusal. MINORs: the trend line joins pairs the day list refuses; the zero sentence shows no confidence level; a refusal sentence chosen by substring-matching `compare()`'s label; three tallies printed as n; the basis cannot tell a headline set from decision 4's block (recorded for METHODOLOGY, benign) | ACCEPTED WITH CONDITIONS: the MAJOR and four MINORs are the first items of row D5 (same files, same work) |
| P1, P2 (oversight) | spend control: the in-app scheduler, the run-now route, the status panel | cost-sentinel 2026-09-19 (independent, on `2c86c5c`) | **NOT SAFE TO ARM.** The logic is sound (repeated presses cannot exceed the cap; two processes run the day once under `claimLocalDay`; a restart does not re-run; loopback only; no test starts the real scheduler). The design is not: `local-tick.ts:83-84` treats an unset `GRADER_DAILY_LOOP` as armed, and the ceiling and live flag it also checks already hold on the owner's machine for the hand-started flow, so arming took no deliberate act; the oversight session confirmed the unset-is-armed branch first-hand, that nothing is tracked and that the app is not running. Also: the click-test crawler does not abort `/api/tick/`; P2 shows nothing for a run that died between reserve and settle; two copy and time-zone MINORs. **The arming rule in row P1 was the oversight session's own text; the error is recorded as such** | NOT ACCEPTED: row P1r must land and pass the oversight cost review of its diff first. The owner must not arm until then (see the read-first note in MVP_PLAN.md) |
| E4 | scoring accuracy: the golden worklist, agent-proposed labels, the PROPOSED verdict, the owner's spot-check sheet (branch `mvp/track-e4`) | the oversight session's own mechanical verification over all 184 cases, 2026-09-19; the track's agent was stopped by the usage limit after pushing | 368 of 368 evidence spans sit at their stated offsets counted as JavaScript counts (UTF-16); every `position` and `competitorsMentioned` follows from the stated order; zero cases where the text names the brand and the label says otherwise; no agent label claims a person verified it; no personal data or secret in the committed text (one phone-like hit is an image tag id). The harness has four states and its tests pin that below the target it is NOT_RUN even with a perfect spot-check, that agent labels alone can never read PASS, and that PASS needs a signed check of the seeded 30 agreeing on 29. Findings for the scoring-rule owner, none changed by the track: subject `mentioned` and `mentionCount` agree on all 191; by-case agreement 87.5%; ChatGPT 94.0% and Gemini 92.3% under the bar; 51 field disagreements in 26 cases, mostly competitor short forms the alias tables do not list ("Dynamics 365", "Zoho", "Close", "Monday") and one review platform: the evidenced input to row E1. The corpus held 185 usable answers, not 360, so the set is 191 of 300 and G2 honestly stays NOT_RUN | ACCEPTED pending its gate on the merged track; the owner's part is the 30-case sheet, `docs/runbooks/golden-spot-check.md` |
| D1 (oversight) | rendered metrics and the client standard: the agency portfolio from real scans | stats-reviewer 2026-09-19 (independent, on `4e5f7cd`) | **Sound with conditions, no BLOCKER.** Wilson verified against the statsmodels grid; every row carries its own n from the block actually scored; every figure goes through `packages/stats`; the trend is `compare()`'s verdict with all five guards, in words, no arrow or colour; nothing sorts or colours by point estimate; no import of the deleted fixture and no hard-coded client anywhere in the app; a never-scanned client shows no number; a tracked row words the instruction ("set to be re-checked daily until D") and the page says nothing schedules a cycle yet, so no row claims collection on an unarmed machine; the C3 review's missing own-set label is on the row at both depths; one component set, the client page is the same record. MAJORs: the stated reason for not ranking is false for two clients in one category; a bare Precision letter; a second, sequentially re-run significance test drives "needs a look" undisclosed. Seven MINORs. Basis question answered: two different brands can carry one basis string, so the basis guard is not what prevents a cross-domain comparison (needs an ADR) | ACCEPTED WITH CONDITIONS: row D1r. The oversight session decided MAJOR 3 conservatively (last-pair verdict only; a sustained-decline detector is a post-G0 METHODOLOGY item); the owner may overrule |

| C3r item 1 | METHODOLOGY: the basis names the sample (the fingerprint in the custom tail, `sameBasis`, `compareCycles`) | **Builder-invoked** stats-reviewer 2026-09-19, independent, on the uncommitted change, told not to trust the builder's notes; it ran the real modules through its own probe scripts | committable with fixes. **One MAJOR, fixed before the commit with the failing case first:** the builder's first draft de-duplicated the list before hashing, but a cycle asks one cell per list ENTRY (`scan.ts` `customCellsFor`), so two lists weighting the same questions differently, both "3 prompts, version 1", still shared one basis string: the defect the item exists to close, left open on the same segment, and the ADR's sentence claiming otherwise was false about the code. Repeats are now kept (`basis.ts` `promptSetFingerprint`; the reviewer's own pair at `basis.test.ts`). MINORs fixed: two difference lines claimed a history nothing checked (now state what the strings show); the published recipe omitted a step (now whole, held to `node:crypto` for every list including an empty-normalising prompt); the normaliser's version is inside the hash; the collision bound quoted was the wrong one. Deferred to the next commit and landed there: a same-version re-score may not restamp a basis; the day list says when a version is the same questions saved again. Verified clean by the reviewer: every cross-cycle comparison goes through the rule, the head-to-head correctly does not, decision-4 block bases and headline bases cannot meet, the written-out SHA-256 agrees with Node over lengths 0 to 260 and a 100 kB input, no cache key moves | NOT YET ACCEPTED by the oversight session; ⚠️ stays (METHODOLOGY) |
| C3r items 2 to 14 | statistics wording (2, 3, 4, 9 and the re-score, the readers, the served-set notice); spend control (12, 13, 14, and 5, 8, 11 as spend-adjacent) | **Builder-invoked** stats-reviewer and cost-sentinel 2026-09-19, independent, in parallel, on the uncommitted change set | **stats-reviewer:** no BLOCKER; three MAJOR, all fixed: the zero sentence printed the outward-rounded frequency beside the nearest-rounded percentage, two numbers for one bound (25% beside 20.4% at fifteen answers; now the one figure the rail shows, `zero-mentions.tsx`); "They will be asked from the next check, on <date>" promised a run nothing schedules for an untracked or expiring domain (now "can run from", `served-set.ts`); the day list printed basis notation, "the prompt count (17 against 0)" beside "your 6 prompts" (now plain words from `basisChangeWords`, `lib/cycles.ts` `whyInPlainWords`). Seven MINOR, all fixed: the cycle tally still stood for the rate's n in three more places; the scope sentence promised a comparison the floor forbids; the trend's prose tied the line to the verdict; a thin scan's one fact was listed against each rival; on the Grader the zero sentence sat below the rail; the evidence reader did not make the re-score's check of a re-read set (`customBasisMismatch`, now both); `dayMarker` had no direct test (now five, including the precision fallback across a revert). NOTEs recorded for the statistics owner: `MIN_N_FOR_COMPARISON` is PROVISIONAL and is now stated as "at least 30" on every line of an own-set trend; `formatBounds` rounds to nearest, so a printed upper limit can sit fractionally below the computed bound (A7's follow-up, row D4); the day list gives the interval in numeric form (D5); the record has no served-set notice of its own. **cost-sentinel:** **one MAJOR, fixed with the failing case first:** item 12 unified the DECISION and not the LOCK; the operator's command wrote `tracked.json` under the record store's lock and the route under the ledger document's, so with the app open and the command in a shell both could pass the ceiling or lose the other's entry; the command now takes the document's own lock (`ledger-doc.ts` `updateFileLedgerSync`) and both run one fold (`due.ts` `foldTracked`), foreign entries verbatim. Traced clean: every per-cycle bound (the due list's cost, the daily cap, the run allowance, the quota pre-check) is sized on the SET's real cell count, never on the environment's smaller figure; the repeated-question throw is before the first cell and before any charge; the cached path's extra read reaches no network and no authoring; the audit copy cannot loop; nothing else starts the app without the loopback bind. Recorded for the owner, not built: `GRADER_MAX_TRACKED_PER_WORKSPACE` has no upper bound of its own (the hard daily ceiling still binds); a collector that throws before its first call is settled at the expected cost, unreachable today | NOT YET ACCEPTED by the oversight session; every ⚠️ stays |
| P1, P2 | spend control: the in-app daily tick (`apps/public/lib/local-tick.ts` `localArming`, `runLocalTick`; `lib/scheduler.ts`; `api/tick/run-now/route.ts`; `load-key.ts` `FILE_FLAGS`; ADR-0017 Amendment 3) and the recorded outcome (`tick-outcomes.ts`) | **Builder-invoked** cost-sentinel 2026-09-19, independent, on the staged change | committable with fixes, no defect in the arming. Verified by it: fail-closed on every malformed input tried (a ceiling that is not a positive number, a flag that is not exactly true, an expired-only or corrupt tracked list, a fleet by marker or declaration, identity on); no new way for an injected environment variable to arm anything (the session settings inject the master collection flag only, which `localArming` does not ask); the four new dotenv names are read by this path alone, and the signed route and the command still take the loop mode from the environment; the same cap, lease and ledger files as the command, so the app and the command cannot double-admit a host; no firing on start, no second firing on a later edit of the hour (the claim is the calendar day), no claim on a failed write, no timer leak; nothing that spends reads the outcome document. **One MAJOR, fixed:** the route test was safe by convention, not by construction (it runs the real route over the real repository root, so a variable a case does not set falls through to this machine's own dotenv); the loop's off switch is now set in its `beforeEach` for every case. **One MAJOR that is not a code fix, for the owner:** this path turns a flag that used to matter only on a person's click into one component of an UNATTENDED trigger; once the two acts are taken and a domain is tracked, any app left running or started from any session collects at the local hour with no further confirmation, bounded by one cycle per host per day and the daily cap; recorded in ADR-0017 Amendment 3. MINOR recorded there: a run refused at ARMING leaves no line in the outcome document (the record states the same fixed sentence live on opening), while a run refused deeper, by the live gates, is recorded. | NOT YET ACCEPTED by the oversight session; ⚠️ stays until the owner reads `local-tick.ts` and Amendment 3 |
| D1 | statistics: rendered metrics on the agency portfolio (`apps/public/lib/portfolio.ts`, `components/portfolio-row.tsx`, `app/agency/page.tsx`) | **Builder-invoked** stats-reviewer 2026-09-19, independent, on the staged change; it re-implemented Wilson and the effective alpha in a scratch script and enumerated every state a row can print | committable with fixes; **six MAJOR, all fixed with a failing case first:** (1) "checked daily" was stated as a fact on the one page with no schedule caveat, where nothing is registered or armed: now worded as the instruction ("set to be re-checked daily until...") with `SCHEDULE_FACT` once above the rows; (2) the notice "every number... this machine really collected" was false of the bundled reference scan and of fixture cycles, and no row marked either: the notice is narrowed and both row states say what they are; (3) the not-a-ranking caveat was shown only when a row had its own set, though every client in its own category is the normal state, and the order note ended "two clients whose ranges overlap are not first and second", which invites the inverse: now unconditional, "no two rows on this page are compared with each other, whether their ranges overlap or not"; (4) a zero row had no zero sentence, the defect C3r item 3 fixed on the record: the shared sentence is on the row; (5) a fallback category was printed as if it had been determined: marked as the queued row and the record mark it; (6) the attention order looked at the last pair only, so fourteen daily checks drifting 60% to 25% with every neighbouring pair inside the range read "no real change" and ranked last (measured by the reviewer): ONE further pair is now compared, fixed in advance, the latest against the oldest check in the unbroken comparable run, never across a boundary. Nine MINOR, fixed: an unreadable status silent and ranked with the fine; "really fell" claiming more than the test does; "the day before" for non-adjacent days (the shared sentence in `basis.ts`); a status read before midnight UTC; no noun after the percentage; an undated scan filed as queued; the pool's prompt count beside a row's own; "your prompts" in agency chrome; unlabelled UTC days. Verified clean by it: the row prints the rate's own n and never the cycle's tally; no delta, arrow or colour; the order leaks no rate and no code path compares one client with another; "higher" and "lower" are true by construction. **For the owner:** a set below the comparison floor can never rise in the attention order (C3's open methodology question, now with a second consequence); the further pair is a second, non-adjacent comparison in a human-owned area; the floor and the design effect are PROVISIONAL pending G0 | NOT YET ACCEPTED by the oversight session; ⚠️ stays (statistics) |

## Open, not yet reviewable
- C3r landed 2026-09-19 with three builder-invoked independent passes (rows above); the oversight session's own pass is outstanding. C3 itself: see its two rows.
- The merge into `main` was done by C0 (2026-09-15): main merged into the branch in a worktree (six conflicts resolved, the manifest renumbered 0008), then the branch merged into `main` with a merge commit. Row C0 above.
