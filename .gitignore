# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Node
node_modules/
dist/
coverage/
.swarm/

# Pilot raw payloads (rule R4: raw answers do not belong in git or Postgres)
services/collector/pilot/data/

# Next.js build output
.next/
out/
next-env.d.ts

# Personal local editor config (Obsidian vault settings — not project state)
.obsidian/

# Collected answers and ledgers from the local Grader runner. Raw payloads never
# go in git for the same reason they never go in Postgres (R4), and the ledger is
# a spend record for one machine, not shared state.
services/grader/data*/

# Screenshot output from scripts/shoot.mjs. Captures are build artifacts:
# they are regenerated on demand and would otherwise land in every diff.
shots/

# TypeScript incremental build cache — a build artifact, never source.
# It was tracked by mistake and showed up as a modified file after every build.
*.tsbuildinfo
